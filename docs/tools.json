[
  {
    "category": "assets",
    "description": "[assets] List every asset (any UClass) under `path`. Asset-registry-backed; O(1) per asset. Use when you don't know the asset's UClass — reach for `list_blueprints` / `list_materials` / etc. instead when you know the type up front (less to filter on the agent side). Returns `[{asset_path, name, class_name}, ...]`. `sort=name` or `sort=path` for deterministic ordering.",
    "input_schema": {
      "properties": {
        "cursor": {
          "description": "Opaque pagination cursor (base64). Alternative to `offset` — pass the cursor returned from a previous call to advance to the next page. Eventually deprecates `offset`. Invalid cursor → -32602.",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "limit": {
          "description": "Optional cap on the number of items returned (after offset). Use to keep responses small on big projects.",
          "type": "integer"
        },
        "offset": {
          "description": "Optional 0-based offset into the result array. Pair with `limit` for paging through long lists.",
          "type": "integer"
        },
        "path": {
          "description": "Content path filter, e.g. /Game/UI. Defaults to /Game.",
          "type": "string"
        },
        "recursive": {
          "description": "Descend into subfolders. Default true.",
          "type": "boolean"
        },
        "sort": {
          "description": "Optional result ordering. `natural` (default) preserves the order the backend returned. `name` sorts by the `name` field of each entry; `path` sorts by `asset_path`. Sort applies BEFORE limit/offset, so paging works against the sorted view.",
          "enum": [
            "natural",
            "name",
            "path"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_assets",
    "output_schema": {
      "items": {
        "properties": {
          "asset_path": {
            "type": "string"
          },
          "class_name": {
            "type": "string"
          },
          "name": {
            "type": "string"
          }
        },
        "required": [
          "asset_path",
          "class_name"
        ],
        "type": "object"
      },
      "type": "array"
    }
  },
  {
    "category": "assets",
    "description": "[assets] Find assets whose name or package path contains `query` (case-insensitive). Scoped to `path` (defaults to /Game). Use this instead of shelling out to `Get-ChildItem` / `find` — asset registry is O(N) once and lives in memory. Returns a paginated envelope `{total, count, offset, has_more, next_cursor, results:[{asset_path, name, class_name}, ...]}`. Capped at 50 results per page by default (a broad `query` can match thousands); pass `limit` to change the page size and `next_cursor` (or `offset`) to fetch the next page. `total` is the full match count so you know how many pages remain.",
    "input_schema": {
      "properties": {
        "cursor": {
          "description": "Opaque pagination cursor (base64). Alternative to `offset` — pass the cursor returned from a previous call to advance to the next page. Eventually deprecates `offset`. Invalid cursor → -32602.",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "limit": {
          "description": "Optional cap on the number of items returned (after offset). Use to keep responses small on big projects.",
          "type": "integer"
        },
        "offset": {
          "description": "Optional 0-based offset into the result array. Pair with `limit` for paging through long lists.",
          "type": "integer"
        },
        "path": {
          "description": "Scope path. Defaults to /Game.",
          "type": "string"
        },
        "query": {
          "description": "Substring matched (case-insensitive) against the asset's short name or full package path.",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "name": "find_asset",
    "output_schema": {
      "properties": {
        "count": {
          "minimum": 0,
          "type": "integer"
        },
        "has_more": {
          "type": "boolean"
        },
        "next_cursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "offset": {
          "minimum": 0,
          "type": "integer"
        },
        "path": {
          "type": "string"
        },
        "query": {
          "type": "string"
        },
        "results": {
          "items": {
            "properties": {
              "asset_path": {
                "type": "string"
              },
              "class_name": {
                "type": "string"
              },
              "name": {
                "type": "string"
              }
            },
            "required": [
              "asset_path",
              "class_name"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "total": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "total",
        "count",
        "has_more",
        "results"
      ],
      "type": "object"
    }
  },
  {
    "category": "blueprint",
    "description": "[blueprint] List Blueprint assets under a content path. Defaults to /Game. On big projects this can return thousands of entries — use `limit`/`offset` to page, and `fields` (e.g. [\"asset_path\"]) to drop columns you don't need. `sort=path` orders by asset path.",
    "input_schema": {
      "properties": {
        "cursor": {
          "description": "Opaque pagination cursor (base64). Alternative to `offset` — pass the cursor returned from a previous call to advance to the next page. Eventually deprecates `offset`. Invalid cursor → -32602.",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "limit": {
          "description": "Optional cap on the number of items returned (after offset). Use to keep responses small on big projects.",
          "type": "integer"
        },
        "offset": {
          "description": "Optional 0-based offset into the result array. Pair with `limit` for paging through long lists.",
          "type": "integer"
        },
        "path": {
          "description": "Content path filter, e.g. /Game/AI. Defaults to /Game.",
          "type": "string"
        },
        "sort": {
          "description": "Optional result ordering. `natural` (default) preserves the order the backend returned. `name` sorts by the `name` field of each entry; `path` sorts by `asset_path`. Sort applies BEFORE limit/offset, so paging works against the sorted view.",
          "enum": [
            "natural",
            "name",
            "path"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_blueprints",
    "output_schema": {
      "items": {
        "properties": {
          "asset_path": {
            "type": "string"
          },
          "parent_class": {
            "type": "string"
          }
        },
        "required": [
          "asset_path"
        ],
        "type": "object"
      },
      "type": "array"
    }
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Read top-level metadata for a Blueprint: parent class, interfaces, variables, function/graph summaries, macros. Pass `fields` (e.g. [\"parent_class\", \"variables[].name\"]) to project just what you need — full payloads can be many KB on busy BPs.\n\n**Typed-BP hint:** this tool reads the BP layer common to every asset type, but specialized assets carry extra structure not surfaced here. Prefer the typed reader when you need it: `read_widget_blueprint` (UMG widget tree), `read_anim_blueprint` (state machines + anim graph), `read_behavior_tree` (BT node hierarchy). `read_blueprint` still works on those assets — just returns the union without the asset-specific structure.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_blueprint",
    "output_schema": null
  },
  {
    "category": "assets",
    "description": "[assets] Read ANY object instance by package path — including a level-placed actor stored in its own external package under `/<Mount>/__ExternalActors__/...` (World Partition / One-File-Per-Actor), which `read_blueprint` can't open. Returns the object's class/name, and for actors the label + transform + owning level, plus `overrides` — the properties this instance changed relative to its archetype (exported-text values). Use this to inspect a misconfigured *placed instance* (the common real-world bug) rather than the Blueprint class it came from. `fields` projects the response (e.g. [\"object_class\",\"overrides[].name\"]).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "Package path of the instance — e.g. an external-actor path /Game/__ExternalActors__/Maps/L_X/0/AB/GUID, or any /Game/... UObject.",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_actor_instance",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "is_actor": {
          "type": "boolean"
        },
        "is_external": {
          "type": "boolean"
        },
        "label": {
          "type": "string"
        },
        "object_class": {
          "type": "string"
        },
        "object_name": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        },
        "override_count": {
          "minimum": 0,
          "type": "integer"
        },
        "overrides": {
          "items": {
            "properties": {
              "name": {
                "type": "string"
              },
              "type": {
                "type": "string"
              },
              "value": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "value"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "owning_level": {
          "type": "string"
        },
        "transform": {
          "type": "object"
        }
      },
      "required": [
        "object_class",
        "is_actor",
        "overrides"
      ],
      "type": "object"
    }
  },
  {
    "category": "cpp",
    "description": "[cpp] Convert a BP function to BPIR (Blueprint Intermediate Representation) — a versioned JSON AST that's the pivot for BP ↔ source-language conversions. Pattern-matches K2 nodes (Branch / Cast / Sequence / VariableSet / CallFunction / FunctionResult) into structured statements + expressions. Anything unrecognized appears as `{unsupported: {...}}` in the body, AND in a top-level `unsupported_nodes` summary for quick \"what couldn't I represent?\" inspection.\n\nPair with `transpile_function` (BPIR → C++) for the full BP→source pipeline. The BPIR returned here is also valid input for `compile_function` (existing tool) — round-trip BP → BPIR → BP works for the patterns BPIR covers cleanly.\n\n**Gated:** the MCP server's process env must include `BP_READER_ALLOW_TRANSPILE=1`. Off by default — when disabled, returns `{ok: false, error: 'transpile_disabled', hint: ...}`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "function_name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "function_name"
      ],
      "type": "object"
    },
    "name": "decompile_function",
    "output_schema": null
  },
  {
    "category": "cpp",
    "description": "[cpp] Whole-class BPIR extraction: variables + interfaces + every function's BPIR. Returns `{kind: \"class\", ...}` doc — the input shape `transpile_blueprint` expects for full UCLASS C++ generation. Per-function decompile failures don't tank the whole call; failed functions appear with `<decompile-failure>` markers in their unsupported_nodes.\n\n**Gated:** the MCP server's process env must include `BP_READER_ALLOW_TRANSPILE=1`. Off by default — when disabled, returns `{ok: false, error: 'transpile_disabled', hint: ...}`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "decompile_blueprint",
    "output_schema": null
  },
  {
    "category": "cpp",
    "description": "[cpp] Convert a Blueprint function to C++ source. Composes decompile_function (BP → BPIR) + C++ codegen (BPIR → source). Default `mode=readable` emits annotated C++ (type names + valid blocks, UCLASS/UFUNCTION shown as comments); for compilable .h/.cpp pairs, use `transpile_blueprint`. Unsupported nodes appear as `// TODO[bpr-unsupported]` comments + a `notes` array the agent can iterate over.\n\n**Gated:** the MCP server's process env must include `BP_READER_ALLOW_TRANSPILE=1`. Off by default — when disabled, returns `{ok: false, error: 'transpile_disabled', hint: ...}`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "function_name": {
          "type": "string"
        },
        "mode": {
          "description": "\"readable\" (default) emits annotated C++ for review; \"compilable\" emits drop-in .h/.cpp pairs (use transpile_blueprint for whole-class output).",
          "enum": [
            "readable",
            "compilable"
          ],
          "type": "string"
        },
        "target_lang": {
          "description": "Target language. Currently C++ only; the BPIR pivot is designed to extend to Lua / Python / JS later.",
          "enum": [
            "cpp"
          ],
          "type": "string"
        },
        "use_operator_aliases": {
          "description": "Render +, ==, && etc. instead of UKismetMathLibrary calls. Default true.",
          "type": "boolean"
        }
      },
      "required": [
        "asset_path",
        "function_name"
      ],
      "type": "object"
    },
    "name": "transpile_function",
    "output_schema": null
  },
  {
    "category": "cpp",
    "description": "[cpp] Convert a whole Blueprint class to a compilable UE C++ .h/.cpp pair. Composes decompile_blueprint + CppClassEmit: emits a UCLASS declaration with UPROPERTY decls (Replicated / EditAnywhere / Category specifiers inferred from BP variable metadata), UFUNCTION decls (BlueprintCallable + Category from BP function metadata), function bodies, and GetLifetimeReplicatedProps() registration when any variable is Replicated.\n\nClass name follows UE convention: a BP named \"BP_Enemy\" with parent ACharacter becomes \"ABP_Enemy_Generated\". The suffix is configurable via class_name_suffix (default \"_Generated\"); pass \"\" if you want the class to drop in place of the BP entirely.\n\nReturns header + impl source strings, suggested filenames, and a `notes` array listing every unsupported BP construct encountered (timelines, latent actions, etc.) for the agent to triage.\n\n**Gated:** the MCP server's process env must include `BP_READER_ALLOW_TRANSPILE=1`. Off by default — when disabled, returns `{ok: false, error: 'transpile_disabled', hint: ...}`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "category_default": {
          "description": "Fallback UPROPERTY Category when the BP variable doesn't carry one. Empty -> no Category specifier emitted.",
          "type": "string"
        },
        "category_remap": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "BP category -> output category map. Applied before category_default. Useful for normalizing common BP categories like \"Default\" to a project's house category.",
          "type": "object"
        },
        "class_name_prefix": {
          "description": "Inserted between UE's type letter (A/U/I) and the BP's CamelCased base name. Empty preserves legacy `ABP_Enemy` form.",
          "type": "string"
        },
        "class_name_suffix": {
          "description": "Default \"_Generated\". Empty drops in place of the BP.",
          "type": "string"
        },
        "delegate_typedef_pattern": {
          "description": "Pattern for derived multicast-delegate typedefs. `{Name}` is the variable name. Default `F{Name}` -> `FOnReady` for var `OnReady`. Use `F{Name}Delegate` for the `FOnReadyDelegate` house style.",
          "type": "string"
        },
        "module_api_macro": {
          "description": "E.g. \"MYGAME_API\". Empty for bare class decl.",
          "type": "string"
        },
        "target_lang": {
          "enum": [
            "cpp"
          ],
          "type": "string"
        },
        "uclass_meta": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra UCLASS() meta=(K=V, ...) entries. E.g. {\"PrioritizeCategories\":\"MyGame\"} folds into the UCLASS macro.",
          "type": "object"
        },
        "use_operator_aliases": {
          "type": "boolean"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "transpile_blueprint",
    "output_schema": null
  },
  {
    "category": "cpp",
    "description": "[cpp] Write a transpiled .h/.cpp file into the project's Source/ tree. Confined by the plugin to paths under <ProjectDir>/Source/ — anything else is rejected. Pair with `transpile_blueprint`: pass the `header_source` / `impl_source` strings the transpile returned, plus the destination paths under your game module's Source dir.\n\nAfter all files are written, run UBT (or use the editor's Live Coding) to compile the new class — the BP can then reparent to the C++ class for hybrid workflows.\n\n**Gated:** the MCP server's process env must include `BP_READER_ALLOW_TRANSPILE=1`. Off by default — when disabled, returns `{ok: false, error: 'transpile_disabled', hint: ...}`.",
    "input_schema": {
      "properties": {
        "content": {
          "description": "File content — typically transpile_blueprint's header_source or impl_source.",
          "type": "string"
        },
        "create_dirs": {
          "description": "Create parent directories if missing. Default true.",
          "type": "boolean"
        },
        "path": {
          "description": "Absolute destination path under <ProjectDir>/Source/.",
          "type": "string"
        }
      },
      "required": [
        "path",
        "content"
      ],
      "type": "object"
    },
    "name": "write_generated_source",
    "output_schema": null
  },
  {
    "category": "cpp",
    "description": "[cpp] Parse a C++ function (or bare body) into a BPIR document — the inverse of `transpile_function`. The returned BPIR can be fed straight to `compile_function` to materialize a BP graph from the source.\n\nTwo input forms:\n  1. Full definition: `bool TakeDamage(float Damage) { ... }`. Signature is parsed for inputs/outputs/return type.\n  2. Bare body: `{ ... }` or `<stmts>`. Pass `signature` (a BPIR function shell with name/inputs/outputs/locals already populated) so the parser knows how to scope variables.\n\nSubset accepted: if/else, range-based for, while, switch + case + default, return, break, continue, expression statements (assignment / call / compound-assign), local declarations, plus expressions over identifiers / qualified names / literals / function calls / member access (./->) / array index / Cast<T>() / this / unary (!, -) / binary (arithmetic / comparison / logical / assign).\n\nErrors are reported as `<line>:<col>: <message>`; the produced BPIR is validated against the schema before return — a malformed parse surfaces as a clear error rather than corrupt downstream graphs.\n\n**Gated:** the MCP server's process env must include `BP_READER_ALLOW_TRANSPILE=1`. Off by default — when disabled, returns `{ok: false, error: 'transpile_disabled', hint: ...}`.",
    "input_schema": {
      "properties": {
        "signature": {
          "description": "Optional BPIR function-doc shell (name/inputs/outputs/locals) when `source` is a bare body.",
          "type": "object"
        },
        "source": {
          "description": "C++ source: full function definition or bare body.",
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "name": "parse_cpp_function",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Tiny orientation response for a Blueprint: parent class plus counts of variables, functions, graphs, macros, and interfaces. Use this BEFORE `read_blueprint` when you don't yet know how big the BP is — saves loading the full payload to find out it has hundreds of variables.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "summarize_blueprint",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Fetch a Blueprint graph (nodes + connections) by name. Defaults to EventGraph. Big graphs are big — pass `fields` (e.g. [\"nodes[].title\", \"nodes[].kind\"]) to drop fields you don't need, or `summary: true` to drop per-node pin arrays and the connections list in one shot.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "graph_name": {
          "description": "Graph name. Defaults to \"EventGraph\".",
          "type": "string"
        },
        "summary": {
          "description": "When true, omits per-node pin arrays and the top-level connections array. Keeps {id, kind, title, comment, position} per node. Default TRUE (lean reads); pass false for the full graph with pin arrays + connections.",
          "type": "boolean"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_graph",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Lightweight probe for a Blueprint graph — returns node count + kind histogram + connection count without any node/pin/connection detail. Use before `get_graph` to decide whether a graph is worth reading in full (event graphs on busy BPs can be tens of KB). Defaults to EventGraph.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        },
        "graph_name": {
          "description": "Graph name. Defaults to \"EventGraph\".",
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "peek_graph",
    "output_schema": {
      "properties": {
        "by_kind": {
          "additionalProperties": {
            "type": "integer"
          },
          "type": "object"
        },
        "connections_count": {
          "minimum": 0,
          "type": "integer"
        },
        "name": {
          "type": "string"
        },
        "nodes_count": {
          "minimum": 0,
          "type": "integer"
        },
        "type": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "nodes_count",
        "by_kind"
      ],
      "type": "object"
    }
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Fetch a Blueprint function: signature (inputs/outputs), locals, and body graph. Use `fields` to project (e.g. [\"inputs[].name\", \"outputs[].name\"] for just the signature). The body graph is under the `graph` key — use `graph.nodes[].id` (not `body.nodes[].id`) in `fields` paths. Use `summary: true` to drop per-node pin arrays and the graph's connections list.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "function_name": {
          "description": "Function name as it appears in the blueprint.",
          "type": "string"
        },
        "summary": {
          "description": "When true, the body graph's nodes drop their pin arrays and the graph's connections list is omitted. Function signature (inputs/outputs/locals) is unchanged. Default TRUE (lean reads); pass false for the full body graph.",
          "type": "boolean"
        }
      },
      "required": [
        "asset_path",
        "function_name"
      ],
      "type": "object"
    },
    "name": "get_function",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] List the SCS components (StaticMeshComponent, LightComponent, child actors, etc.) attached to a blueprint, with parent/child hierarchy. Each entry: {name, class, parent, is_root}. Supports `fields`/`limit`/`offset`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        },
        "cursor": {
          "description": "Opaque pagination cursor (base64). Alternative to `offset` — pass the cursor returned from a previous call to advance to the next page. Eventually deprecates `offset`. Invalid cursor → -32602.",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "limit": {
          "description": "Optional cap on the number of items returned (after offset). Use to keep responses small on big projects.",
          "type": "integer"
        },
        "offset": {
          "description": "Optional 0-based offset into the result array. Pair with `limit` for paging through long lists.",
          "type": "integer"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_components",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] List all member variables on a Blueprint, with type, default, category, and replication state. Big BPs can have 100+ variables — use `fields` (e.g. [\"name\", \"type.category\"]) and `limit`/`offset` to keep responses small.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        },
        "cursor": {
          "description": "Opaque pagination cursor (base64). Alternative to `offset` — pass the cursor returned from a previous call to advance to the next page. Eventually deprecates `offset`. Invalid cursor → -32602.",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "limit": {
          "description": "Optional cap on the number of items returned (after offset). Use to keep responses small on big projects.",
          "type": "integer"
        },
        "offset": {
          "description": "Optional 0-based offset into the result array. Pair with `limit` for paging through long lists.",
          "type": "integer"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "list_variables",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Search nodes within a Blueprint by class or title (case-insensitive substring). Optional `kind` further filters by the K2 extras kind, e.g. CallFunction, VariableGet, Event, CustomEvent, DynamicCast, MacroInstance, FunctionEntry, FunctionResult. Supports `fields`/`limit`/`offset` — typical use is `fields=[\"id\", \"title\", \"kind\"]` to keep responses small.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        },
        "cursor": {
          "description": "Opaque pagination cursor (base64). Alternative to `offset` — pass the cursor returned from a previous call to advance to the next page. Eventually deprecates `offset`. Invalid cursor → -32602.",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Optional. K2 extras `kind` to match exactly (case-insensitive).",
          "type": "string"
        },
        "limit": {
          "description": "Optional cap on the number of items returned (after offset). Use to keep responses small on big projects.",
          "type": "integer"
        },
        "offset": {
          "description": "Optional 0-based offset into the result array. Pair with `limit` for paging through long lists.",
          "type": "integer"
        },
        "query": {
          "description": "Substring matched against node class or title. Pass an empty string to match any node when filtering by `kind` only.",
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "query"
      ],
      "type": "object"
    },
    "name": "find_node",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Create a new Blueprint asset under `/Game/...` extending `parent_class`. `parent_class` accepts short names (\"Actor\", \"ACharacter\") and full UClass paths (\"/Script/Engine.Actor\"). Idempotent: calling with an existing asset returns `{ok:true, already_existed:true}` without modifying it. Pair with `apply_ops` to create + populate a BP in one batch.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "Must start with /Game/. Example: /Game/AI/BP_Boss",
          "type": "string"
        },
        "blueprint_type": {
          "description": "Optional EBlueprintType name (e.g. BPTYPE_Interface, BPTYPE_FunctionLibrary, BPTYPE_MacroLibrary). Omit for a normal BP derived from parent_class.",
          "type": "string"
        },
        "parent_class": {
          "description": "UClass short name or full path. Example: Actor or /Script/Engine.Actor",
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "parent_class"
      ],
      "type": "object"
    },
    "name": "create_blueprint",
    "output_schema": {
      "properties": {
        "already_existed": {
          "type": "boolean"
        },
        "asset_path": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        },
        "parent_class": {
          "type": "string"
        }
      },
      "required": [
        "ok",
        "asset_path"
      ],
      "type": "object"
    }
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Copy every node in `graph` from the `source` Blueprint into the same-named graph of the `target` Blueprint, preserving wiring. The `target` must already have a graph named `graph` (create the function/event graph first with `add_function` if needed). Used by the BP-recreate flow to reproduce a graph wholesale. Recompiles + saves the target.",
    "input_schema": {
      "properties": {
        "graph": {
          "description": "Graph name present in both BPs. Example: EventGraph or MyFunction",
          "type": "string"
        },
        "source": {
          "description": "Source BP package path. Example: /Game/AI/BP_Src",
          "type": "string"
        },
        "target": {
          "description": "Target BP package path. Example: /Game/AI/BP_Dst",
          "type": "string"
        }
      },
      "required": [
        "source",
        "target",
        "graph"
      ],
      "type": "object"
    },
    "name": "clone_graph",
    "output_schema": {
      "properties": {
        "imported_nodes": {
          "type": "integer"
        },
        "ok": {
          "type": "boolean"
        }
      },
      "required": [
        "ok",
        "imported_nodes"
      ],
      "type": "object"
    }
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Add `interface` to the implemented-interfaces list of the `asset` Blueprint, generating the stub function graphs the interface requires. `interface` accepts a BP-interface package path (/Game/...) or a native UInterface path (/Script/...). Recompiles + saves the BP.",
    "input_schema": {
      "properties": {
        "asset": {
          "description": "BP package path. Example: /Game/AI/BP_Boss",
          "type": "string"
        },
        "interface": {
          "description": "Interface path. Example: /Game/Interfaces/BPI_Damageable or /Script/Engine.SomeInterface",
          "type": "string"
        }
      },
      "required": [
        "asset",
        "interface"
      ],
      "type": "object"
    },
    "name": "implement_interface",
    "output_schema": {
      "properties": {
        "ok": {
          "type": "boolean"
        }
      },
      "required": [
        "ok"
      ],
      "type": "object"
    }
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Add a member variable to a Blueprint class. For graph nodes use `add_node`; for function parameters use `add_function_input`/`add_function_output`. `type` accepts either a shorthand string (\"float\", \"int\", \"bool\", \"string\", \"object:Actor\", \"struct:FVector\", \"[]float\", \"{string:int}\") or the canonical BPPinType object {category, sub_category, sub_category_object, is_array, is_set, is_map}. For a TMap the main fields are the KEY and a nested `value_type` {category, sub_category, sub_category_object} object gives the VALUE (e.g. {category:object, sub_category_object:/Script/Engine.Pawn, is_map:true, value_type:{category:object, sub_category_object:/Game/.../IndicatorDescriptor.IndicatorDescriptor_C}}). Idempotent: if a variable with this name already exists, returns {ok:true, already_existed:true} without modifying it.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "category": {
          "type": "string"
        },
        "default_value": {
          "type": "string"
        },
        "editable": {
          "type": "boolean"
        },
        "name": {
          "type": "string"
        },
        "replicated": {
          "type": "boolean"
        },
        "type": {
          "description": "Type shorthand string or BPPinType object — see tool description."
        }
      },
      "required": [
        "asset_path",
        "name",
        "type"
      ],
      "type": "object"
    },
    "name": "add_variable",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Move a K2 node (by GUID) to (x, y) inside a Blueprint graph. Recompiles + saves the BP.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "graph_name": {
          "type": "string"
        },
        "node_id": {
          "description": "Node GUID",
          "type": "string"
        },
        "x": {
          "type": "integer"
        },
        "y": {
          "type": "integer"
        }
      },
      "required": [
        "asset_path",
        "graph_name",
        "node_id",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "set_node_position",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Delete a K2 node by GUID. Breaks any links into/out of it. Recompiles + saves.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "graph_name": {
          "type": "string"
        },
        "node_id": {
          "description": "Node GUID",
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "graph_name",
        "node_id"
      ],
      "type": "object"
    },
    "name": "delete_node",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Spawn a new K2 (Blueprint) node in a Blueprint graph. NOT for UMG widgets (`add_widget`), Behavior Tree (`add_bt_node`), AnimBP state (`add_anim_state`), StateTree (`add_state_tree_state`), Material expressions (`add_material_expression`), or Level Sequence tracks (`add_sequence_track`). `kind` is one of: Common kinds: Branch, Sequence, VariableGet, VariableSet, CallFunction, CustomEvent, Cast, Self, MakeArray, MakeStruct, FormatText, Knot, GetSubsystem. Call list_node_kinds for the authoritative set + required arg(s) per kind. Kind-specific args: VariableGet/VariableSet -> `variable`; CallFunction -> `function` + `function_owner` (UClass path or short name); CustomEvent -> `event_name`; Cast -> `target_class`; GetSubsystem -> `subsystem_class`. Returns {ok, node_id, pins:[...]}. The `pins` array carries each pin's name/guid/direction/type so you can wire it without a follow-up get_graph call.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "event_name": {
          "type": "string"
        },
        "function": {
          "type": "string"
        },
        "function_owner": {
          "type": "string"
        },
        "graph_name": {
          "type": "string"
        },
        "kind": {
          "type": "string"
        },
        "struct_type": {
          "type": "string"
        },
        "subsystem_class": {
          "type": "string"
        },
        "target_class": {
          "type": "string"
        },
        "variable": {
          "type": "string"
        },
        "x": {
          "type": "integer"
        },
        "y": {
          "type": "integer"
        }
      },
      "required": [
        "asset_path",
        "graph_name",
        "kind",
        "x",
        "y"
      ],
      "type": "object"
    },
    "name": "add_node",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Fetch a single K2 node by GUID inside a graph. Returns the node's class, title, position, pins, and links — same shape as one entry from `get_graph`'s nodes array, minus the round-trip cost of fetching every node. Pairs with `find_node` (which gives you the GUID) for targeted inspection.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "graph_name": {
          "type": "string"
        },
        "node_id": {
          "description": "Node GUID",
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "graph_name",
        "node_id"
      ],
      "type": "object"
    },
    "name": "get_node",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Find Blueprints under `path` that match a structural query: extend `parent_class`, override `function_name`, and/or implement `interface`. All filters are optional but at least one must be set. Returns a list of `{asset_path, parent_class, matched: [...]}` entries. Replaces the manual `list_blueprints` + N×`read_blueprint` loop the agent would otherwise walk.\n\n**Performance:** `parent_class` is cheap (matches against the asset summary). `function_name` and `interface` each require a full read of every candidate BP, so on a large project they can time out. When either is set, also pass `parent_class` to narrow the candidate set first — the request will be rejected otherwise.",
    "input_schema": {
      "properties": {
        "cursor": {
          "description": "Opaque pagination cursor (base64). Alternative to `offset` — pass the cursor returned from a previous call to advance to the next page. Eventually deprecates `offset`. Invalid cursor → -32602.",
          "type": "string"
        },
        "fields": {
          "description": "Optional response projection. Each entry is a dotted field path; use `[]` to apply the path to every element of an array. Example: [\"name\", \"variables[].name\"] returns just the BP name and the names of its variables. Omit to get the full payload. Field names must match the response keys: variable flags are `is_editable`, `is_replicated`, `expose_on_spawn`, `rep_condition`, `rep_notify_func`. Common shorthands are aliased (`editable`, `replicated`, `exposed`, and any bare `foo` for an `is_foo` flag), but an unrecognized name still projects nothing.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "function_name": {
          "description": "Match BPs that define a function with this name (e.g. \"BeginPlay\").",
          "type": "string"
        },
        "interface": {
          "description": "Match BPs that implement this interface (short name).",
          "type": "string"
        },
        "limit": {
          "description": "Optional cap on the number of items returned (after offset). Use to keep responses small on big projects.",
          "type": "integer"
        },
        "offset": {
          "description": "Optional 0-based offset into the result array. Pair with `limit` for paging through long lists.",
          "type": "integer"
        },
        "parent_class": {
          "description": "Match BPs whose parent class equals this (short name OR /Script/... path).",
          "type": "string"
        },
        "path": {
          "description": "Content-path scope. Defaults to /Game.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "find_overriders",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Walk a Blueprint's function bodies (and optionally top-level graphs) for nodes referencing variables / intra-BP functions that no longer exist on the BP. Catches the renamed-or-deleted-but-node-still-references scenario.\n\nReturns `{asset_path, dangling: [...], total}`. Each dangling entry is `{graph, node_id, node_class, title, missing, symbol_type}`. Empty `dangling` array means clean.\n\nCoverage limits: external (cross-BP) function calls aren't validated — we can only confirm intra-BP calls. UMG bind_widget references need a future plugin-side validator. Inherited parent-class members (e.g. PlayerCameraManager) are treated as valid, not dangling.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "UE asset path, e.g. /Game/AI/BP_Enemy",
          "type": "string"
        },
        "include_top_level": {
          "description": "Also fetch + scan top-level graphs (EventGraph, ConstructionScript). Costs N×get_graph on the wire. Default true.",
          "type": "boolean"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "find_dangling_references",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "dangling": {
          "items": {
            "properties": {
              "graph": {
                "type": "string"
              },
              "missing": {
                "type": "string"
              },
              "node_class": {
                "type": "string"
              },
              "node_id": {
                "type": "string"
              },
              "symbol_type": {
                "enum": [
                  "variable",
                  "function"
                ],
                "type": "string"
              },
              "title": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        },
        "total": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "asset_path",
        "dangling",
        "total"
      ],
      "type": "object"
    }
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Connect two K2 pins. `from_pin` and `to_pin` accept either a pin GUID (preferred — see get_graph) or a pin name. The schema's pin compatibility rules are enforced; on failure, the error message includes the actual pin types so the caller can self-correct.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "from_node": {
          "type": "string"
        },
        "from_pin": {
          "type": "string"
        },
        "graph_name": {
          "type": "string"
        },
        "to_node": {
          "type": "string"
        },
        "to_pin": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "graph_name",
        "from_node",
        "from_pin",
        "to_node",
        "to_pin"
      ],
      "type": "object"
    },
    "name": "wire_pins",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Set the literal default value on a node's input pin. UE has no first-class literal node — values flow in as the consumer pin's default. `pin` accepts a pin GUID (preferred — get_graph) or a pin name. `value` is the literal as a string (the editor parses it per the pin's type: numbers, bool true/false, strings raw, vectors as `(X=...,Y=...,Z=...)`). This is what `compile_function`'s `{lit: ...}` expression emits under the hood; reach for `set_pin_default` directly when you already have the node + pin and just want the default set.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "graph_name": {
          "description": "Graph holding the node. EventGraph if omitted.",
          "type": "string"
        },
        "node_id": {
          "description": "Target node's GUID.",
          "type": "string"
        },
        "pin": {
          "description": "Pin GUID (preferred) or pin name. Alias: `pin_name` is also accepted, for parity with the apply_ops set_pin_default op.",
          "type": "string"
        },
        "value": {
          "description": "Literal value as a string; editor parses per pin type.",
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "node_id",
        "value"
      ],
      "type": "object"
    },
    "name": "set_pin_default",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Change a Blueprint member variable's type without delete + re-add. UE rewires every VariableGet / VariableSet node that references the variable in place — existing graphs survive. For a brand-new variable, use add_variable instead. `type` accepts the same shorthand strings (\"float\", \"object:Actor\", \"[]float\") and BPPinType objects as add_variable.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "type": {
          "description": "Type shorthand string or BPPinType object."
        }
      },
      "required": [
        "asset_path",
        "name",
        "type"
      ],
      "type": "object"
    },
    "name": "retype_variable",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Change the My-Blueprint-panel category label on a Blueprint member variable (the \"Stats\" / \"Combat\" group header in the BP editor). Empty `category` clears the label back to default. For a brand-new variable, pass `category` to add_variable instead — this tool is for retroactive edits.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "category": {
          "description": "Empty clears the category back to default.",
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name"
      ],
      "type": "object"
    },
    "name": "set_variable_category",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] File-level duplicate: source BP at `asset_path` → new BP at `dest_asset_path`. Both must be under /Game/. Idempotent: if the destination already exists, returns {ok:true, already_existed:true} without overwriting. The new BP starts identical to the source (same vars, functions, graphs, components) and is registered with the asset registry so a follow-up apply_ops batch can mutate it.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "description": "Source BP under /Game/. Must exist.",
          "type": "string"
        },
        "dest_asset_path": {
          "description": "Destination BP under /Game/. Must NOT exist (idempotent: returns already_existed:true if it does).",
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "dest_asset_path"
      ],
      "type": "object"
    },
    "name": "duplicate_blueprint",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Remove a Blueprint member variable by name. Recompiles + saves.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name"
      ],
      "type": "object"
    },
    "name": "delete_variable",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Rename a Blueprint member variable. Updates references in graphs. Recompiles + saves.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "new_name": {
          "type": "string"
        },
        "old_name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "old_name",
        "new_name"
      ],
      "type": "object"
    },
    "name": "rename_variable",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Create a new BP function graph with the given name. Returns {ok, function_name, already_existed}. Use add_function_input / add_function_output to declare its signature. Idempotent — calling with an existing name returns already_existed:true.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name"
      ],
      "type": "object"
    },
    "name": "add_function",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Add an input parameter to an existing function. `type` accepts either a shorthand string (e.g. \"float\", \"object:Actor\") or a BPPinType object.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "function_name": {
          "type": "string"
        },
        "param_name": {
          "type": "string"
        },
        "type": {
          "description": "Type shorthand string or BPPinType object."
        }
      },
      "required": [
        "asset_path",
        "function_name",
        "param_name",
        "type"
      ],
      "type": "object"
    },
    "name": "add_function_input",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Add an output parameter to an existing function. Spawns a FunctionResult node if there isn't one yet. `type` accepts either a shorthand string (e.g. \"float\", \"object:Actor\") or a BPPinType object.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "function_name": {
          "type": "string"
        },
        "param_name": {
          "type": "string"
        },
        "type": {
          "description": "Type shorthand string or BPPinType object."
        }
      },
      "required": [
        "asset_path",
        "function_name",
        "param_name",
        "type"
      ],
      "type": "object"
    },
    "name": "add_function_output",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Delete a Blueprint function graph by name.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name"
      ],
      "type": "object"
    },
    "name": "delete_function",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Change a Blueprint member variable's default value (string form, as displayed in the Details panel — e.g. \"100.0\" for a float, \"true\"/\"false\" for a bool). Pass empty string to clear.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "default_value": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name",
        "default_value"
      ],
      "type": "object"
    },
    "name": "set_variable_default",
    "output_schema": null
  },
  {
    "category": "discover",
    "description": "[discover] List the `kind` values that `add_node` accepts, with required extras for each. Pure metadata — no backend call.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_node_kinds",
    "output_schema": null
  },
  {
    "category": "discover",
    "description": "[discover] List the canonical BPPinType.category values + container modifiers. Useful when constructing the `type` argument for add_variable. Pure metadata — no backend call.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_pin_categories",
    "output_schema": null
  },
  {
    "category": "discover",
    "description": "[discover] Force-terminate the backing editor daemon process. In shared-daemon mode (the default), one daemon serves every MCP session against a given project — so this affects EVERY session, not just yours. Other sessions' next call simply spawns a fresh daemon (cold-start cost ~5–30 s depending on project size). The original use cases still work: releases the project's file locks (DDC, asset registry, .uasset handles) so you can launch the full UE editor, or forces a fresh spawn after upgrading the plugin. Pair with BP_READER_READ_ONLY=1 if you want to keep the MCP server running for queries while you work in the editor.\n\nReturns {ok, was_running, hint}. Idempotent: calling when no daemon is alive returns was_running:false without erroring.\n\nGUARD: because this affects every session, you must pass `force_shared:true` to actually tear it down — a default-deny safeguard against accidentally killing other sessions' daemon.",
    "input_schema": {
      "properties": {
        "force_shared": {
          "description": "Required confirmation. shutdown_daemon terminates the daemon shared by EVERY MCP session on this project, not just yours. Must be true to proceed; omitting it (default-deny) returns an error explaining the blast radius rather than tearing down.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "name": "shutdown_daemon",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Auto-position the nodes in a Blueprint graph so they don't overlap. v1 is a column-grid layout computed from exec connectivity — readable but not as tidy as UE's built-in graph-tidy. Plugin-side integration with KismetGraphSchema's actual tidy pass is tracked separately. Returns {ok, placed: <n>}.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "col_width": {
          "description": "Horizontal spacing between columns. Default 400.",
          "type": "integer"
        },
        "graph_name": {
          "type": "string"
        },
        "row_height": {
          "description": "Vertical spacing between rows. Default 200.",
          "type": "integer"
        }
      },
      "required": [
        "asset_path",
        "graph_name"
      ],
      "type": "object"
    },
    "name": "auto_layout_graph",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] Read the project's `.uproject` file and return parsed metadata (project name, EngineAssociation, category, description, plus the raw JSON for anything else). Useful for orienting an agent to which project + engine version it's working against.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_project_metadata",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] Save every dirty package the editor has loaded. With `dirty_only=true` (default), clean packages are skipped — fast no-op when nothing's changed. Returns count saved + any failed asset paths.",
    "input_schema": {
      "properties": {
        "dirty_only": {
          "description": "Default true. Set false to save every loaded package, dirty or not.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "name": "save_all",
    "output_schema": {
      "properties": {
        "failed": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ok": {
          "type": "boolean"
        },
        "saved": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "ok",
        "saved"
      ],
      "type": "object"
    }
  },
  {
    "category": "asset",
    "description": "[asset] Move or rename an asset. `dest_asset_path` is the full destination package path — pass the same folder with a different leaf for a rename, or a different folder to move. Both must be under `/Game/`. Updates the asset registry and fixes references in other assets. Returns the redirector count if UE created any.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "dest_asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "dest_asset_path"
      ],
      "type": "object"
    },
    "name": "move_asset",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] Delete an asset. Refuses by default if other assets reference it; set `force=true` to delete anyway (leaves dangling references as null objects). Returns the list of referencing assets when the refusal fires so the caller can act on them.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "force": {
          "description": "Default false. Set true to delete even when references exist.",
          "type": "boolean"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "delete_asset",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] Create a content-browser folder under `/Game/`. Idempotent — returns `{already_existed:true}` when the folder is already present.",
    "input_schema": {
      "properties": {
        "folder_path": {
          "description": "Package path of the folder to create, e.g. /Game/AI/Boss.",
          "type": "string"
        }
      },
      "required": [
        "folder_path"
      ],
      "type": "object"
    },
    "name": "create_folder",
    "output_schema": null
  },
  {
    "category": "data table",
    "description": "[data table] List all UDataTable assets under a content path. Mirrors `list_blueprints` but filters for UDataTable. Defaults to `/Game`.",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_data_tables",
    "output_schema": null
  },
  {
    "category": "data table",
    "description": "[data table] Load a DataTable and return its row-struct type, column names, and every row's field values. Useful for orienting an agent to an existing data table before mutating it.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_data_table",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Execute a UE console command (e.g. `stat unit`, `showflag.bones 1`, `r.ScreenPercentage 75`). Returns whatever the command echoed to the log.",
    "input_schema": {
      "properties": {
        "command": {
          "type": "string"
        }
      },
      "required": [
        "command"
      ],
      "type": "object"
    },
    "name": "console_command",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] One-call situational awareness: what assets are open in the editor (which is active), the currently-loaded level and its dirty state, the viewport camera transform, currently-selected actors, and whether PIE is running. Use this BEFORE starting a multi-step edit to ground the agent on what the human is looking at right now. Live-editor only — returns empty/null fields in commandlet mode (no editor UI). The selected_actors field duplicates `get_selected_actors` for convenience; prefer this tool for first-call orientation.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_editor_state",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Execute an Unreal Python script in the live editor. Wrapped in a single undo transaction so mutations are reversible. **Gated:** the MCP server's process env must include `BP_READER_ALLOW_PYTHON=1`. Off by default — arbitrary Python has full access to the `unreal.*` API and bypasses the curated tool surface. When disabled, returns `{ok: false, error: 'python_disabled', hint: ...}`. Returns the captured stdout/stderr in `log[]` plus the script's command result. For curated authoring, prefer the named BP/asset/material/widget tools.",
    "input_schema": {
      "properties": {
        "code": {
          "description": "Unreal Python source. Can be multi-line. Has access to the full `unreal.*` API.",
          "type": "string"
        }
      },
      "required": [
        "code"
      ],
      "type": "object"
    },
    "name": "run_python_script",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] Return every asset (package path) that REFERENCES this asset. Useful for impact analysis before renaming, deleting, or refactoring. Sourced from the asset registry's dependency graph (in-memory; fast). Inverse of `get_dependencies`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_referencers",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] Return every asset (package path) that this asset DEPENDS on. Inverse of `get_referencers`. Useful for understanding what loads when this asset loads.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_dependencies",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] Read a UE config (.ini) value. `file` is one of \"Engine\" (default), \"Game\", \"Input\", \"Editor\", \"EditorPerProjectIni\", or a full path. Returns `{exists, value}` — `exists:false` if the key isn't set in any branch of the resolved file.",
    "input_schema": {
      "properties": {
        "file": {
          "description": "Engine|Game|Input|Editor|EditorPerProjectIni, or full path. Defaults to Engine.",
          "type": "string"
        },
        "key": {
          "type": "string"
        },
        "section": {
          "description": "Ini section, e.g. \"/Script/Engine.RendererSettings\"",
          "type": "string"
        }
      },
      "required": [
        "section",
        "key"
      ],
      "type": "object"
    },
    "name": "read_config_value",
    "output_schema": null
  },
  {
    "category": "asset",
    "description": "[asset] Write a UE config (.ini) value + flush to disk. `file` defaults to Engine (DefaultEngine.ini). Returns `{previous_value, value}` so the caller can verify the change. **Destructive** — writes the project's ini file on disk. Use only when the agent has explicit intent to modify project settings.",
    "input_schema": {
      "properties": {
        "file": {
          "type": "string"
        },
        "key": {
          "type": "string"
        },
        "section": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "section",
        "key",
        "value"
      ],
      "type": "object"
    },
    "name": "set_config_value",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Trigger a lighting build (Lightmass) on the currently-loaded level. Async — returns once the build is QUEUED, not once it finishes. `quality` is one of \"Preview\", \"Medium\", \"High\", \"Production\" (default Production). Poll `read_output_log` for completion: Lightmass emits \"Lighting build complete\" or \"Lighting build failed\" when it finishes. Live-editor only.",
    "input_schema": {
      "properties": {
        "quality": {
          "enum": [
            "Preview",
            "Medium",
            "High",
            "Production"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "build_lighting",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Read a console variable's current value. Returns `{exists, value, help}` — `exists:false` if the CVar isn't registered.",
    "input_schema": {
      "properties": {
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "get_cvar",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Set a UE console variable (e.g. `r.ScreenPercentage`). Runtime-style; for Blueprint variable defaults use `set_variable_default`. Forces `ECVF_SetByCode` priority — overrides values set from ini files / scalability settings.",
    "input_schema": {
      "properties": {
        "name": {
          "type": "string"
        },
        "value": {
          "description": "Stringified value; UE coerces to the CVar's type.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "value"
      ],
      "type": "object"
    },
    "name": "set_cvar",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Start a Play-In-Editor session. `mode` is one of `selected_viewport` (default), `new_editor_window`, `standalone`, `vr_preview`. Most useful with the live backend.",
    "input_schema": {
      "properties": {
        "mode": {
          "enum": [
            "selected_viewport",
            "new_editor_window",
            "standalone",
            "vr_preview"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "pie_start",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] End the active PIE session. No-op when PIE isn't running.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "pie_stop",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] List every asset the user currently has open in some asset editor (one entry per editor window). Returns `[{asset_path, asset_class, last_activation_seconds}, ...]`. Sort by `last_activation_seconds` to see most-recently-used first. Requires a live editor — commandlet mode throws.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_open_assets",
    "output_schema": {
      "items": {
        "properties": {
          "asset_class": {
            "type": "string"
          },
          "asset_path": {
            "type": "string"
          },
          "last_activation_seconds": {
            "type": "number"
          }
        },
        "type": "object"
      },
      "type": "array"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Asset whose editor was most recently activated — the one the user is most likely focused on. Returns `{asset_path, asset_class, last_activation_seconds}`. `asset_path` is empty when no asset editor is open. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_active_asset",
    "output_schema": {
      "properties": {
        "asset_class": {
          "type": "string"
        },
        "asset_path": {
          "type": "string"
        },
        "last_activation_seconds": {
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Blueprint compile status — wraps `UBlueprint::Status` into a stable string. Useful after a write op to confirm the BP is compile-clean before further mutations. Values: \"uncompiled\", \"dirty\", \"good\", \"warning\", \"error\", \"compiling\", \"unknown\". Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_compile_status",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "last_compile_error": {
          "type": "string"
        },
        "status": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] List loaded packages with unsaved changes. Pairs with `save_all` — agents that just mutated multiple BPs use this to confirm there's nothing unsaved before disconnecting. Each entry flags `is_content` so the agent can ignore editor-only / engine packages. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_dirty_packages",
    "output_schema": {
      "items": {
        "properties": {
          "is_content": {
            "type": "boolean"
          },
          "package_name": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "type": "array"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Title + Slate widget-class of the currently-focused top-level window. Useful for \"is the user in the BP editor right now?\" routing. Returns `{title, class_name}`. Both empty when no Slate window has focus. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_focused_window",
    "output_schema": {
      "properties": {
        "class_name": {
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Is Play-In-Editor running right now? Returns `{is_playing, mode, instance_count}`. `mode` is the PIE mode string (selected_viewport / new_editor_window / standalone / vr_preview) or empty when not playing. instance_count >= 2 for multi-PIE (Client/Server split). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_pie_state",
    "output_schema": {
      "properties": {
        "instance_count": {
          "type": "integer"
        },
        "is_playing": {
          "type": "boolean"
        },
        "mode": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Is a modal Slate window blocking input right now? Returns `{is_open, title}`. Common modals: confirm-deletion, asset-picker, save-as. Agents should refuse mutation ops when is_open=true — the editor is gated on user input. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_modal_state",
    "output_schema": {
      "properties": {
        "is_open": {
          "type": "boolean"
        },
        "title": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Slate widget the user is currently typing into / focused on. Finer-grained than get_focused_window: \"the user is in the BP graph search box\" vs \"the user is on the BP editor window\". Returns `{widget_type, parent_window_title}`. Both empty when no widget has focus. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_focused_widget",
    "output_schema": {
      "properties": {
        "parent_window_title": {
          "type": "string"
        },
        "widget_type": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Open the asset editor for the given asset (Blueprint, Material, etc.). Idempotent — opening an already-open asset brings the existing editor window to front. `asset_path` is package form: `/Game/AI/BP_Foo`. Returns `{opened, asset_path}`. `opened` is false when the asset couldn't be loaded. Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "open_asset_editor",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "opened": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Close all editor windows for the given asset. Idempotent — closing an asset with no editor open is a no-op (`closed:false`, no error). `asset_path` is package form. Returns `{closed, asset_path}`. Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "close_asset_editor",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "closed": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Persona / animation editor selection state. v1 is a documented stub returning `valid:false` for all paths — surfacing the wire shape `{valid, asset_path, selected_bone_index, selected_socket_name}` so callers can see the planned contract. Implementation deferred: Persona editors use multi-inheritance (FPersonaAssetEditorToolkit + IHasPersonaToolkit) that UE's no-RTTI environment can't safely cross-cast. Future fix requires a sidecar registry hooked into UAssetEditorSubsystem::OnAssetOpenedInEditor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_anim_editor_state",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "selected_bone_index": {
          "type": "integer"
        },
        "selected_socket_name": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Actors visible in the active level viewport — frustum-tested against the camera, filtered by class substring (`class_filter`, case-sensitive) and max distance (`max_distance` in cm; 0 = no limit). Skips hidden actors. Per-actor `{name, label, actor_class, world_x/y/z, distance_cm, screen_x/y, has_screen_pos}`. `screen_x/y` are normalized [0,1]; `has_screen_pos:false` means the actor is in frustum but projects behind the camera plane. Capped at 500 actors. Requires a live editor.",
    "input_schema": {
      "properties": {
        "class_filter": {
          "type": "string"
        },
        "max_distance": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "name": "get_visible_actors",
    "output_schema": {
      "properties": {
        "actors": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "truncated": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Names of editor layers whose visibility is off (`ULayersSubsystem`). Capped at 500 — `truncated:true` signals a larger set. Pair with `set_layer_visibility` to toggle. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_hidden_layers",
    "output_schema": {
      "properties": {
        "layer_names": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "truncated": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Show or hide an editor layer by name (`ULayersSubsystem`). `valid:false` means no layer with that name exists. Blocked in read-only mode (mutates level-domain state). Requires a live editor.",
    "input_schema": {
      "properties": {
        "layer": {
          "type": "string"
        },
        "visible": {
          "type": "boolean"
        }
      },
      "required": [
        "layer",
        "visible"
      ],
      "type": "object"
    },
    "name": "set_layer_visibility",
    "output_schema": {
      "properties": {
        "layer": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        },
        "visible": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Show or hide an actor in the editor viewport by name via `SetIsTemporarilyHiddenInEditor` (does not dirty the package). `valid:false` means no actor with that name was found. Blocked in read-only mode. Pairs with `get_hidden_actors`. Requires a live editor.",
    "input_schema": {
      "properties": {
        "name": {
          "type": "string"
        },
        "visible": {
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "visible"
      ],
      "type": "object"
    },
    "name": "set_actor_visibility",
    "output_schema": {
      "properties": {
        "name": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        },
        "visible": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Set the active level viewport's view mode. Accepts `Lit`, `Unlit`, `Wireframe`, `BrushWireframe`, `LightingOnly`, `LightComplexity`, `ShaderComplexity`, `StationaryLightOverlap`, `LightmapDensity`, `LitLightmapDensity`, `ReflectionOverride`, `CollisionPawn`, `CollisionVisibility` (case-insensitive). `valid:false` means no viewport or an unrecognized mode. Read counterpart: `get_view_mode`. Requires a live editor.",
    "input_schema": {
      "properties": {
        "mode": {
          "type": "string"
        }
      },
      "required": [
        "mode"
      ],
      "type": "object"
    },
    "name": "set_view_mode",
    "output_schema": {
      "properties": {
        "mode": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Set the transform gizmo mode in the active level viewport: `translate`, `rotate`, or `scale` (case-insensitive). `valid:false` means no viewport or an unrecognized mode. Read counterpart: `get_gizmo_state`. Requires a live editor.",
    "input_schema": {
      "properties": {
        "mode": {
          "type": "string"
        }
      },
      "required": [
        "mode"
      ],
      "type": "object"
    },
    "name": "set_gizmo_mode",
    "output_schema": {
      "properties": {
        "mode": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Enable or disable realtime rendering in the active level viewport. `valid:false` means no viewport; `is_realtime` echoes the resulting state. Read counterpart: `get_viewport_realtime`. Requires a live editor.",
    "input_schema": {
      "properties": {
        "enabled": {
          "type": "boolean"
        }
      },
      "required": [
        "enabled"
      ],
      "type": "object"
    },
    "name": "set_viewport_realtime",
    "output_schema": {
      "properties": {
        "is_realtime": {
          "type": "boolean"
        },
        "ok": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Saved viewport camera bookmarks (Ctrl-1..9 poses) from the world settings. Returns only populated slots: `{slot, loc_x/y/z, pitch, yaw, roll}`. `max_slots` is the allocated bookmark array size. Pair with `goto_camera_bookmark`. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_camera_bookmarks",
    "output_schema": {
      "properties": {
        "bookmarks": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "max_slots": {
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Jump the active viewport camera to a saved bookmark slot (0-based). `jumped:false` means the slot is empty or no viewport is focused. View-state only — allowed in read-only mode. Requires a live editor.",
    "input_schema": {
      "properties": {
        "slot": {
          "type": "integer"
        }
      },
      "required": [
        "slot"
      ],
      "type": "object"
    },
    "name": "goto_camera_bookmark",
    "output_schema": {
      "properties": {
        "jumped": {
          "type": "boolean"
        },
        "ok": {
          "type": "boolean"
        },
        "slot": {
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Hit-proxy target under the cursor (actor/surface/component). v1 stub: resolving the hit proxy needs a render-thread readback + RTTI cross-cast that isn't safe out-of-process — returns `valid:false` until an editor-module sidecar captures hover state. Shape is stable.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_hover_target",
    "output_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        },
        "hit_proxy_type": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Show-only-selected / isolate mode state (UE 5.6+). v1 stub: no stable public accessor for the level-viewport isolate flag — returns `valid:false`. Shape is stable.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_isolate_mode",
    "output_schema": {
      "properties": {
        "isolated": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Names of actors hidden in the editor viewport (temporary or level hide). Capped at 500 — `truncated:true` signals a larger hidden set. Sorted by iteration order, not by name. Pair with `set_actor_visibility` to toggle. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_hidden_actors",
    "output_schema": {
      "properties": {
        "actor_names": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "truncated": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Editor snapping settings: grid + rotation snap toggles, current grid sizes (position + rotation), vertex snapping, snap-to-actor distance. Reads `ULevelEditorViewportSettings`. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_snapping_settings",
    "output_schema": {
      "properties": {
        "actor_snap_distance": {
          "type": "number"
        },
        "current_pos_grid_size": {
          "type": "integer"
        },
        "current_rot_grid_size": {
          "type": "integer"
        },
        "grid_enabled": {
          "type": "boolean"
        },
        "rot_grid_enabled": {
          "type": "boolean"
        },
        "snap_distance": {
          "type": "number"
        },
        "snap_vertices": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Which level viewport has focus right now (1/2/4 layout). Returns `{valid, viewport_index, is_perspective, size_x, size_y}`. `viewport_index` is 0-based across the level-editor viewport clients; `is_perspective` distinguishes perspective from ortho (top/front/side). `valid:false` when no viewport has focus. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_active_viewport",
    "output_schema": {
      "properties": {
        "is_perspective": {
          "type": "boolean"
        },
        "size_x": {
          "type": "integer"
        },
        "size_y": {
          "type": "integer"
        },
        "valid": {
          "type": "boolean"
        },
        "viewport_index": {
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active level viewport's buffer-visualization override (`base_color`, `roughness`, `normals`, `metallic`, etc.). Returns `{valid, mode}`. Empty `mode` means no override — the viewport renders in its current view-mode (Lit/Unlit/etc.). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_buffer_visualization_mode",
    "output_schema": {
      "properties": {
        "mode": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Translation gizmo state for the active level viewport. Returns `{valid, mode, coord_space}`. `mode` is `translate` / `rotate` / `scale` / `translaterotatez` / `2d` / `none`. `coord_space` is `world` or `local`. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_gizmo_state",
    "output_schema": {
      "properties": {
        "coord_space": {
          "type": "string"
        },
        "mode": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active level viewport's realtime flag — whether the viewport renders every frame (true) or only on user interaction (false). Returns `{valid, is_realtime}`. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_viewport_realtime",
    "output_schema": {
      "properties": {
        "is_realtime": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active level viewport's camera settings: FOV, camera speed multiplier, near/far clip planes. Returns `{valid, fov, camera_speed, near_clip, far_clip}`. `far_clip` is 0 when no override is set (engine default). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_viewport_camera_settings",
    "output_schema": {
      "properties": {
        "camera_speed": {
          "type": "number"
        },
        "far_clip": {
          "type": "number"
        },
        "fov": {
          "type": "number"
        },
        "near_clip": {
          "type": "number"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Niagara system / emitter / module editor selection. v1 is a documented stub returning `valid:false` for all paths. Niagara editors use specialized toolkit types in the Niagara plugin's editor module — safe cross-cast without RTTI requires a sidecar registry. Same upgrade path as `get_anim_editor_state`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_niagara_module_selection",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "selected_module_names": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Generic curve editor selection. v1 is a documented stub returning `valid:false`. The curve editor is a SCurveEditor widget hosted inside 5+ different editors (Anim, Sequencer, Particle, Material), so there's no single instance keyed on asset_path. Upgrade requires per-host-editor tracking of the active FCurveEditor pointer.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_curve_editor_selection",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "selected_curve_names": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "selected_key_count": {
          "type": "integer"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Sequencer state for an open ULevelSequence editor. Returns `{valid, asset_path, playhead_seconds, playback_status, playback_range_start_seconds, playback_range_end_seconds}`. `playback_status` is one of `stopped`, `playing`, `scrubbing`, `jumping`, `stepping`, `paused`, `unknown`. `valid:false` when the level sequence isn't open in the editor. Selection state (tracks/sections/keys) not yet exposed; v1 covers playhead + range + status only. Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_sequencer_state",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "playback_range_end_seconds": {
          "type": "number"
        },
        "playback_range_start_seconds": {
          "type": "number"
        },
        "playback_status": {
          "type": "string"
        },
        "playhead_seconds": {
          "type": "number"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Currently-active camera in PIE — reads `APlayerCameraManager::GetViewTarget()` from the first PIE player controller. Returns `{valid, actor_name, loc_x/y/z, pitch/yaw/roll, fov}`. `valid:false` when PIE isn't running or no view target is set. Useful for inspecting what a cinematic sequence is showing the player right now.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_cinematic_camera",
    "output_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        },
        "fov": {
          "type": "number"
        },
        "loc_x": {
          "type": "number"
        },
        "loc_y": {
          "type": "number"
        },
        "loc_z": {
          "type": "number"
        },
        "pitch": {
          "type": "number"
        },
        "roll": {
          "type": "number"
        },
        "valid": {
          "type": "boolean"
        },
        "yaw": {
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Static mesh editor preview state: forced LOD level and currently-displayed LOD index. `current_lod_level` is -1 when on auto-LOD; an explicit integer otherwise. `current_lod_index` is the LOD being rendered right now (resolved by the editor's distance/auto selection). `valid:false` when the static mesh editor for the asset isn't open.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_mesh_preview_state",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "current_lod_index": {
          "type": "integer"
        },
        "current_lod_level": {
          "type": "integer"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Material editor selection state. Returns `{valid, asset_path, selected_node_count, selected_expression_classes}`. `selected_expression_classes` lists short class names of currently-selected material expression nodes (e.g. `MaterialExpressionAdd`, `MaterialExpressionTextureSample`). `valid:false` when the material editor for the asset isn't open. Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_material_editor_state",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "selected_expression_classes": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "selected_node_count": {
          "type": "integer"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Selection state for a UMG Widget Blueprint editor. Returns `{valid, asset_path, selected_widget_names, current_designer_tab}`. `selected_widget_names` are the template-side widget names (matching the widget hierarchy panel). `valid:false` when the UMG editor for the asset isn't open. Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_umg_editor_state",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "current_designer_tab": {
          "type": "string"
        },
        "selected_widget_names": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Static mesh LOD / triangle / vertex info. Reads directly from the UStaticMesh asset — no editor instance needed. Returns `{valid, asset_path, lod_count, is_nanite_enabled, lods: [{triangle_count, vertex_count, screen_size}]}`. `screen_size` is the LOD-streaming threshold (0.0 when not set on the source model).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_static_mesh_info",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "is_nanite_enabled": {
          "type": "boolean"
        },
        "lod_count": {
          "type": "integer"
        },
        "lods": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Dump scalar/vector/texture parameter values from a UMaterialInstanceConstant asset. No open editor required — reads parameters directly from the asset. Returns `{valid, asset_path, parent_path, scalars, vectors, textures}`. Switch params + RVT params not included as a scope cap.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_material_instance_params",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "parent_path": {
          "type": "string"
        },
        "scalars": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "textures": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        },
        "vectors": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Per-Blueprint-editor state for the named BP asset. Returns `{valid, asset_path, current_graph_name, compile_status, selected_node_ids}`. `selected_node_ids` are FGuid strings; empty when no nodes selected. `compile_status` mirrors the `get_compile_status` enum strings. `valid:true` requires the BP's editor to be open (else still returns compile_status but no selection data). Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_blueprint_editor_state",
    "output_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "compile_status": {
          "type": "string"
        },
        "current_graph_name": {
          "type": "string"
        },
        "selected_node_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Gameplay attributes on the named actor's `UAbilitySystemComponent`. Returns `{valid, actor_name, attributes: [{name, base_value, current_value}]}`. `base_value` is the raw value before active-effect modifications; `current_value` is after. `name` uses 'AttrSet.Attribute' form (e.g. 'LyraHealthSet.Health'). Requires a live editor + GAS plugin.",
    "input_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        }
      },
      "required": [
        "actor_name"
      ],
      "type": "object"
    },
    "name": "list_actor_attributes",
    "output_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        },
        "attributes": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active gameplay effects on the named actor's `UAbilitySystemComponent`. Returns `{valid, actor_name, effects: [{effect_class, stack_count, duration_remaining, level, granted_tags}]}`. `duration_remaining` is -1 for infinite-duration effects, else remaining seconds. Requires a live editor + GAS plugin.",
    "input_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        }
      },
      "required": [
        "actor_name"
      ],
      "type": "object"
    },
    "name": "list_actor_gameplay_effects",
    "output_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        },
        "effects": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Abilities granted to the named actor's `UAbilitySystemComponent`. Returns `{valid, actor_name, abilities: [{ability_class, is_active, level, instanced_count}]}`. `valid:false` when actor doesn't exist or has no ASC. `actor_name` matches the names from `get_selected_actors`. Looks at PIE world first, then editor. Lyra: useful for inspecting a pawn's runtime ability set. Requires a live editor + GAS plugin (always present in Lyra).",
    "input_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        }
      },
      "required": [
        "actor_name"
      ],
      "type": "object"
    },
    "name": "list_actor_abilities",
    "output_schema": {
      "properties": {
        "abilities": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "actor_name": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Owned gameplay tags on the named actor's `UAbilitySystemComponent`. Union of tags granted by abilities, by effects, and loose tags. Returns `{valid, actor_name, tags: ['Status.Buffed', ...]}`. `valid:false` when actor doesn't exist or has no ASC. Requires a live editor + GAS plugin.",
    "input_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        }
      },
      "required": [
        "actor_name"
      ],
      "type": "object"
    },
    "name": "list_actor_gameplay_tags",
    "output_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        },
        "tags": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Enumerate all discovered plugins via IPluginManager. Returns `{plugins: [{name, descriptor_path, category, version, is_enabled, is_built_in, is_content_only}]}`. Includes both enabled and disabled plugins (use `is_enabled` to filter). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_plugins",
    "output_schema": {
      "properties": {
        "plugins": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Read the full .uplugin descriptor for `plugin_name`. Returns `{valid, descriptor}` where `descriptor` is the raw parsed JSON from the .uplugin file (UE's FPluginDescriptor schema — name, version, description, modules, plugins, etc.). `valid:false` when plugin name doesn't resolve. Requires a live editor.",
    "input_schema": {
      "properties": {
        "plugin_name": {
          "type": "string"
        }
      },
      "required": [
        "plugin_name"
      ],
      "type": "object"
    },
    "name": "get_plugin_descriptor",
    "output_schema": {
      "properties": {
        "descriptor": {
          "type": "object"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Plugins that `plugin_name` depends on (extracted from the `Plugins:` array in its descriptor). Returns `{valid, dependencies: [plugin_name, ...]}`. Useful for understanding dependency chains before disabling a plugin. Requires a live editor.",
    "input_schema": {
      "properties": {
        "plugin_name": {
          "type": "string"
        }
      },
      "required": [
        "plugin_name"
      ],
      "type": "object"
    },
    "name": "get_plugin_dependencies",
    "output_schema": {
      "properties": {
        "dependencies": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] List all known Game Feature Plugins (GFPs) and their simplified state. Lyra uses GFPs extensively as its modular content mechanism. Returns `{features: [{plugin_name, plugin_url, state}]}`. `state` is one of: `unknown`, `registered`, `loading`, `loaded`, `active`, `deactivating`. Requires a live editor (GameFeaturesSubsystem only lives in a running engine context).",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_game_features",
    "output_schema": {
      "properties": {
        "features": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Look up a single Game Feature Plugin's state by name (case-insensitive match against `plugin_name`). Returns `{valid, plugin_name, plugin_url, state}`. `valid:false` when the plugin name doesn't resolve. Requires a live editor.",
    "input_schema": {
      "properties": {
        "plugin_name": {
          "type": "string"
        }
      },
      "required": [
        "plugin_name"
      ],
      "type": "object"
    },
    "name": "get_game_feature_state",
    "output_schema": {
      "properties": {
        "plugin_name": {
          "type": "string"
        },
        "plugin_url": {
          "type": "string"
        },
        "state": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Recursive Slate widget tree snapshot. Walks every visible top-level window (or only the one whose title contains `window`) and returns each widget as `{depth, widget_type, text, parent_window}`. `text` is best-effort (extracted from `STextBlock`-like widgets); empty otherwise. Capped at 500 nodes; `truncated:true` when a subtree was cut off. Default max_depth=8. Requires a live editor.",
    "input_schema": {
      "properties": {
        "max_depth": {
          "type": "integer"
        },
        "window": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "ui_snapshot",
    "output_schema": {
      "properties": {
        "nodes": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "truncated": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Locate Slate widgets by visible text. Returns the same `{nodes, truncated}` shape as `ui_snapshot` but filtered: only widgets whose extracted text contains `text` (case-sensitive). `role` (optional) further restricts to widgets whose type name contains the substring (e.g. `Button` matches `SButton`). Capped at 200 hits. Requires a live editor.",
    "input_schema": {
      "properties": {
        "role": {
          "type": "string"
        },
        "text": {
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "ui_find",
    "output_schema": {
      "properties": {
        "nodes": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "truncated": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] List visible top-level Slate windows: `{windows: [{title, widget_type, pos_x/y, size_x/y, is_active}]}`. Lighter-weight alternative to a desktop screenshot when an agent just needs to know which windows are open. Z-order from `GetAllVisibleWindowsOrdered`. Requires a live editor. NOTE: full `take_desktop_screenshot` (multi-window composite image) is deferred — see plan doc Wave 1 remainders.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_desktop_windows",
    "output_schema": {
      "properties": {
        "windows": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Project a 3D world position to the active level viewport's screen space. Returns `{valid, screen_x, screen_y, is_on_screen}`. `screen_x`/`screen_y` are normalized [0,1] across the viewport. `is_on_screen` is true only when the projection is in front of the camera AND inside the viewport. Requires a live editor.",
    "input_schema": {
      "properties": {
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        },
        "z": {
          "type": "number"
        }
      },
      "required": [
        "x",
        "y",
        "z"
      ],
      "type": "object"
    },
    "name": "world_pos_to_screen",
    "output_schema": {
      "properties": {
        "is_on_screen": {
          "type": "boolean"
        },
        "screen_x": {
          "type": "number"
        },
        "screen_y": {
          "type": "number"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Inverse of `world_pos_to_screen`. Cast a ray from screen-normalized [0,1] coordinates out to `max_distance` cm in world space. Returns `{valid, hit, world_x, world_y, world_z, hit_actor_name}`. When `hit` is true, the world coords are the line-trace impact point and `hit_actor_name` identifies the actor. When false, the coords are the ray endpoint. Default max_distance is 10000 cm. Requires a live editor.",
    "input_schema": {
      "properties": {
        "max_distance": {
          "type": "number"
        },
        "screen_x": {
          "type": "number"
        },
        "screen_y": {
          "type": "number"
        }
      },
      "required": [
        "screen_x",
        "screen_y"
      ],
      "type": "object"
    },
    "name": "screen_to_world",
    "output_schema": {
      "properties": {
        "hit": {
          "type": "boolean"
        },
        "hit_actor_name": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        },
        "world_x": {
          "type": "number"
        },
        "world_y": {
          "type": "number"
        },
        "world_z": {
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Currently-selected assets in the Content Browser. Returns `{asset_paths: [/Game/AI/BP_Foo, ...]}` (package form). Multi-select preserved in order. Empty when nothing selected. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_selected_assets",
    "output_schema": {
      "properties": {
        "asset_paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Replace the Content Browser asset selection. `asset_paths` are package form (`/Game/AI/BP_Foo`). Returns the post-call selection so the caller can verify (UI may not have settled instantly — poll if needed). Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "asset_paths"
      ],
      "type": "object"
    },
    "name": "set_selected_assets",
    "output_schema": {
      "properties": {
        "asset_paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Currently-selected folder paths in the Content Browser tree (distinct from assets — folders are navigation rows). Returns `{folder_paths: [...]}` in package form. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_selected_folders",
    "output_schema": {
      "properties": {
        "folder_paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] The folder the Content Browser is currently displaying (its address-bar path). Returns `{current_path}`. Empty when no path is selected. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_content_browser_path",
    "output_schema": {
      "properties": {
        "current_path": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Navigate the Content Browser to `folder_path` (package form, e.g. `/Game/AI/Behaviors`). Returns the post-call current_path for verification. Requires a live editor.",
    "input_schema": {
      "properties": {
        "folder_path": {
          "type": "string"
        }
      },
      "required": [
        "folder_path"
      ],
      "type": "object"
    },
    "name": "set_content_browser_path",
    "output_schema": {
      "properties": {
        "current_path": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Components owned by each currently-selected actor. Returns `[{actor_name, components: [{name, component_class}]}]`. Empty actors array when nothing is selected. Useful when an agent wants to operate on a specific component (mesh, collision, trigger) without first asking the user to drill into the actor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_selected_components",
    "output_schema": {
      "properties": {
        "actors": {
          "items": {
            "properties": {
              "actor_name": {
                "type": "string"
              },
              "components": {
                "items": {
                  "properties": {
                    "component_class": {
                      "type": "string"
                    },
                    "name": {
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "type": "array"
              }
            },
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Read the active level viewport's camera state. Picks the focused level viewport (or first perspective if none has focus). Returns `{valid, loc_x/y/z, pitch/yaw/roll, fov}`. `valid:false` means no level viewport exists (PIE teardown, headless commandlet). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_camera_transform",
    "output_schema": {
      "properties": {
        "fov": {
          "type": "number"
        },
        "loc_x": {
          "type": "number"
        },
        "loc_y": {
          "type": "number"
        },
        "loc_z": {
          "type": "number"
        },
        "pitch": {
          "type": "number"
        },
        "roll": {
          "type": "number"
        },
        "valid": {
          "type": "boolean"
        },
        "yaw": {
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Read the active level viewport's view mode (Lit / Unlit / Wireframe / DetailLighting / ShaderComplexity / LightingOnly / ReflectionOverride / VisualizeBuffer / etc.). Returns `{valid, mode}`. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_view_mode",
    "output_schema": {
      "properties": {
        "mode": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Read the active level viewport's commonly-toggled show flags. Returns a curated subset of FEngineShowFlags' ~100 fields: wireframe, collision, grid, bounds, navigation, atmosphere, fog, lighting, post_processing, antialiasing, shadows. Boolean each. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_show_flags",
    "output_schema": {
      "properties": {
        "antialiasing": {
          "type": "boolean"
        },
        "atmosphere": {
          "type": "boolean"
        },
        "bounds": {
          "type": "boolean"
        },
        "collision": {
          "type": "boolean"
        },
        "fog": {
          "type": "boolean"
        },
        "grid": {
          "type": "boolean"
        },
        "lighting": {
          "type": "boolean"
        },
        "navigation": {
          "type": "boolean"
        },
        "post_processing": {
          "type": "boolean"
        },
        "shadows": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        },
        "wireframe": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active level-editor mode(s) — what tool is the user working in? Returns `{active_modes: [...]}`. Common modes: `EM_Default` (selection), `EM_Placement`, `EM_Landscape`, `EM_Foliage`, `EM_MeshPaint`, `EM_ModelingMode`. Multi-mode is possible (UE supports concurrent modes); primary is element [0]. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_active_editor_mode",
    "output_schema": {
      "properties": {
        "active_modes": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Trigger UE's Live Coding compile + patch. The compile runs asynchronously; Live Coding emits its own progress + result to the editor log (use `read_output_log` to follow).",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "live_coding_compile",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] List the names of currently-selected actors in the level editor. Names are the stable in-package names (not display labels). Empty array when nothing is selected.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_selected_actors",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Replace (or extend) the editor viewport's actor selection. `replace:true` (default) clears existing selection first; `false` adds to it. Returns the post-call selected names so the caller can verify.",
    "input_schema": {
      "properties": {
        "actor_names": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "replace": {
          "type": "boolean"
        }
      },
      "required": [
        "actor_names"
      ],
      "type": "object"
    },
    "name": "set_selection",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Spawn an actor of the given UClass in the current level. `class_path` is the full path (e.g. `/Script/Engine.StaticMeshActor` or a BP class like `/Game/AI/BP_Enemy.BP_Enemy_C`). All transform fields are optional and default to identity.",
    "input_schema": {
      "properties": {
        "class_path": {
          "type": "string"
        },
        "location": {
          "properties": {
            "x": {
              "type": "number"
            },
            "y": {
              "type": "number"
            },
            "z": {
              "type": "number"
            }
          },
          "type": "object"
        },
        "rotation": {
          "properties": {
            "pitch": {
              "type": "number"
            },
            "roll": {
              "type": "number"
            },
            "yaw": {
              "type": "number"
            }
          },
          "type": "object"
        },
        "scale": {
          "properties": {
            "x": {
              "type": "number"
            },
            "y": {
              "type": "number"
            },
            "z": {
              "type": "number"
            }
          },
          "type": "object"
        }
      },
      "required": [
        "class_path"
      ],
      "type": "object"
    },
    "name": "spawn_actor",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Update a placed actor's world transform in the current level. `actor_name` is from `get_selected_actors` or `spawn_actor`'s response. All transform fields are absolute (not delta).",
    "input_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        },
        "location": {
          "type": "object"
        },
        "rotation": {
          "type": "object"
        },
        "scale": {
          "type": "object"
        }
      },
      "required": [
        "actor_name"
      ],
      "type": "object"
    },
    "name": "set_actor_transform",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Destroy an actor by name. Returns `{deleted: false}` if the actor wasn't found.",
    "input_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        }
      },
      "required": [
        "actor_name"
      ],
      "type": "object"
    },
    "name": "delete_actor",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Read recent entries from the editor's output log. The plugin module installs a ring-buffer log sink at startup; this returns up to `limit` of the most recent entries (default 200), optionally filtered by `min_severity` (Display / Log / Warning / Error / Fatal).",
    "input_schema": {
      "properties": {
        "limit": {
          "type": "integer"
        },
        "min_severity": {
          "enum": [
            "Display",
            "Log",
            "Warning",
            "Error",
            "Fatal"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "read_output_log",
    "output_schema": null
  },
  {
    "category": "data table",
    "description": "[data table] Add a row to an existing DataTable. The row name must be unique within the table; existing names return `{already_existed:true}` unless `overwrite:true` is passed. `values` is an object whose keys map to the row struct's field names; values are stringified and coerced via FProperty::ImportText (works for scalars, enums, and structs that round-trip through text). Pair with `read_data_table` to see the row-struct shape before calling.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "overwrite": {
          "description": "Default false. Set true to replace an existing row.",
          "type": "boolean"
        },
        "row_name": {
          "type": "string"
        },
        "values": {
          "description": "Field-name → value map. Values are stringified; ImportText coerces to the property's type.",
          "type": "object"
        }
      },
      "required": [
        "asset_path",
        "row_name",
        "values"
      ],
      "type": "object"
    },
    "name": "add_data_row",
    "output_schema": null
  },
  {
    "category": "data table",
    "description": "[data table] Update a single field on an existing DataTable row. `field_name` must match a property on the row struct; `value` is its string form (ImportText input). Returns the pre-set and post-set ExportText'd values so the caller can verify the coercion landed.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "field_name": {
          "type": "string"
        },
        "row_name": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "row_name",
        "field_name",
        "value"
      ],
      "type": "object"
    },
    "name": "set_data_row_value",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Add a component (StaticMeshComponent, AudioComponent, etc.) to a Blueprint's SimpleConstructionScript tree — author-time component setup, not runtime spawning. For runtime actor spawning use `spawn_actor`. `component_class` is the full UClass path (e.g. `/Script/Engine.StaticMeshComponent`). Pass `parent` to attach as a child of an existing node; omit for root attachment. `socket` applies to SceneComponent children only. Idempotent on `name`: existing names return `{already_existed:true}`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "component_class": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "parent": {
          "type": "string"
        },
        "socket": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name",
        "component_class"
      ],
      "type": "object"
    },
    "name": "add_component",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Remove a component from a Blueprint's SCS tree by name. Returns `{removed:false}` when the component isn't found.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name"
      ],
      "type": "object"
    },
    "name": "remove_component",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Re-parent an SCS component on a Blueprint. Pass `new_parent` to attach the component as a child of that node; pass empty to attach at the SCS root. `socket` applies to SceneComponent children only.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "new_parent": {
          "type": "string"
        },
        "socket": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name"
      ],
      "type": "object"
    },
    "name": "attach_component",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Set a UPROPERTY on a Blueprint component's template (the author-time default values, what the BP Details panel shows for that component). For widget UPROPERTYs use `set_widget_property`; for behavior tree nodes use `set_bt_node_property`. Same string→type coercion as `set_data_row_value` (FProperty::ImportText). Returns pre-set and post-set ExportText'd values for verification, plus `default_value` (the component class-default for this property) and `has_override` — so a `new_value` that equals the default (which exports as the default's text, e.g. \"\" / \"False\") isn't misread as cleared.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "component": {
          "type": "string"
        },
        "property": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "component",
        "property",
        "value"
      ],
      "type": "object"
    },
    "name": "set_component_property",
    "output_schema": null
  },
  {
    "category": "tests",
    "description": "[tests] Trigger UE's automation test framework. `pattern` is the test-name wildcard (e.g. `BlueprintReader.*`, `*Smoke*`); empty means every registered test. The run is async — this tool kicks it off and returns. Use `read_output_log` to follow results, or check `Saved/Automation/index.json` after for the structured report.",
    "input_schema": {
      "properties": {
        "pattern": {
          "description": "Test-name wildcard. Empty = all tests.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "run_automation_tests",
    "output_schema": null
  },
  {
    "category": "material",
    "description": "[material] List all UMaterial / UMaterialInstance assets under a content path. Mirrors `list_blueprints` but filters by class. Defaults to `/Game`.",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_materials",
    "output_schema": null
  },
  {
    "category": "material",
    "description": "[material] Read a material's expression graph: every UMaterialExpression node (id, class, parameter name, x/y), every connection (from expression output → expression input or master-material slot like BaseColor / Roughness), and the names of all exposed scalar/vector parameters.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_material",
    "output_schema": null
  },
  {
    "category": "material",
    "description": "[material] Add a UMaterialExpression node to a Material graph. For Blueprint graph nodes use `add_node`. `expression_class` is the short class name like `MaterialExpressionConstant3Vector`, `MaterialExpressionScalarParameter`, `MaterialExpressionTextureSampleParameter2D`. x/y are graph coordinates. Returns the new expression's id (use in `connect_material_expressions`).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "expression_class": {
          "type": "string"
        },
        "x": {
          "type": "integer"
        },
        "y": {
          "type": "integer"
        }
      },
      "required": [
        "asset_path",
        "expression_class"
      ],
      "type": "object"
    },
    "name": "add_material_expression",
    "output_schema": null
  },
  {
    "category": "material",
    "description": "[material] Wire one expression's output pin to another expression's input pin, or to a master-material slot. Pass empty `to_node` to wire to a master slot (`to_pin` then names the slot, e.g. `BaseColor`, `Metallic`, `Roughness`, `EmissiveColor`, `Normal`).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "from_node": {
          "type": "string"
        },
        "from_pin": {
          "type": "string"
        },
        "to_node": {
          "type": "string"
        },
        "to_pin": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "from_node",
        "from_pin",
        "to_pin"
      ],
      "type": "object"
    },
    "name": "connect_material_expressions",
    "output_schema": null
  },
  {
    "category": "material",
    "description": "[material] Set the default value of a named scalar/vector parameter on a UMaterial (the base material). `value` is the parameter's text representation (scalar: `0.5`; vector: `(R=1,G=0,B=0,A=1)`). For per-instance overrides use `set_material_instance_parameter`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "parameter_name": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "parameter_name",
        "value"
      ],
      "type": "object"
    },
    "name": "set_material_parameter",
    "output_schema": null
  },
  {
    "category": "material",
    "description": "[material] Override a parameter on a UMaterialInstanceConstant. For base-material defaults use `set_material_parameter`. `type` is `scalar`, `vector`, or `texture`; `value` is its text form (scalar `0.5`, vector `(R=...,G=...,B=...,A=...)`, texture `/Game/Textures/T_Foo.T_Foo`).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "parameter_name": {
          "type": "string"
        },
        "type": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "parameter_name",
        "type",
        "value"
      ],
      "type": "object"
    },
    "name": "set_material_instance_parameter",
    "output_schema": null
  },
  {
    "category": "material",
    "description": "[material] Recompile a material's shader code. UE normally compiles incrementally on edit; call this explicitly to flush pending recompiles or recover from a stuck shader compile state.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "compile_material",
    "output_schema": null
  },
  {
    "category": "widget",
    "description": "[widget] Read a UWidgetBlueprint's widget tree: every UWidget node (name, class, parent name) and the root widget's name. Mirrors `get_components` but for UMG.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_widget_blueprint",
    "output_schema": null
  },
  {
    "category": "widget",
    "description": "[widget] Add a UWidget node (Button, TextBlock, etc.) to a UWidgetBlueprint's UMG tree. For Blueprint graph nodes use `add_node`. `widget_class` is the short class name (`Button`, `TextBlock`, `Image`, `VerticalBox`, etc.). `parent_name` empty = becomes the new root (replaces the existing root only if the tree was empty). Otherwise appends as a child of `parent_name`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "parent_name": {
          "type": "string"
        },
        "widget_class": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "widget_class",
        "name"
      ],
      "type": "object"
    },
    "name": "add_widget",
    "output_schema": null
  },
  {
    "category": "widget",
    "description": "[widget] Set a UPROPERTY on a UWidget in a UWidgetBlueprint. For Blueprint component UPROPERTYs use `set_component_property`. `property_name` is the property's name as authored in C++ (`Text`, `ColorAndOpacity`, `Visibility`). `value` is the property's text form (text: a string; FLinearColor: `(R=1,G=0,B=0,A=1)`).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "property_name": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "widget_name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "widget_name",
        "property_name",
        "value"
      ],
      "type": "object"
    },
    "name": "set_widget_property",
    "output_schema": null
  },
  {
    "category": "widget",
    "description": "[widget] Bind a widget's event (e.g. `OnClicked` on a Button) to a named handler function in the widget blueprint's graph. If the handler function doesn't exist, it's created with the event's signature. Pairs with `add_function` if you want to author the handler explicitly first.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "event_name": {
          "type": "string"
        },
        "handler_function": {
          "type": "string"
        },
        "widget_name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "widget_name",
        "event_name",
        "handler_function"
      ],
      "type": "object"
    },
    "name": "bind_widget_event",
    "output_schema": null
  },
  {
    "category": "widget",
    "description": "[widget] Compile a UWidgetBlueprint. Equivalent to clicking Compile in the UMG designer. Returns `{compiled: true|false}` — false means compile failed; check `read_output_log` for errors.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "compile_widget_blueprint",
    "output_schema": null
  },
  {
    "category": "behavior tree",
    "description": "[behavior tree] List UBehaviorTree assets under a content path (default `/Game`).",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_behavior_trees",
    "output_schema": null
  },
  {
    "category": "behavior tree",
    "description": "[behavior tree] Walk a UBehaviorTree's node graph. Returns every node (id, class, kind, parent) and the root node id.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_behavior_tree",
    "output_schema": null
  },
  {
    "category": "behavior tree",
    "description": "[behavior tree] Add a node (composite/decorator/service/task) to a UBehaviorTree. NOT for Blueprint graph nodes (`add_node`). `node_kind` is `composite` / `decorator` / `service` / `task`; `node_class` is the short class name (e.g. `BTComposite_Selector`, `BTTask_MoveTo`, `BTDecorator_Blackboard`). Empty `parent_node_id` becomes the root composite (only allowed for the first composite added).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "node_class": {
          "type": "string"
        },
        "node_kind": {
          "type": "string"
        },
        "parent_node_id": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "node_kind",
        "node_class"
      ],
      "type": "object"
    },
    "name": "add_bt_node",
    "output_schema": null
  },
  {
    "category": "behavior tree",
    "description": "[behavior tree] Set a UPROPERTY on a UBehaviorTree node (e.g. MoveTo's `AcceptableRadius`, Blackboard decorator's `KeyName`). `value` is the property's text form.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "node_id": {
          "type": "string"
        },
        "property_name": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "node_id",
        "property_name",
        "value"
      ],
      "type": "object"
    },
    "name": "set_bt_node_property",
    "output_schema": null
  },
  {
    "category": "behavior tree",
    "description": "[behavior tree] Compile a behavior tree (recompiles + marks the asset dirty). Returns `{compiled: true|false}`.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "compile_behavior_tree",
    "output_schema": null
  },
  {
    "category": "data asset",
    "description": "[data asset] List all UDataAsset subclass instances under a content path. Mirrors `list_blueprints` but filters by base class.",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_data_assets",
    "output_schema": null
  },
  {
    "category": "data asset",
    "description": "[data asset] Read every UPROPERTY on a UDataAsset. Returns the asset's class + a `{property: stringified_value}` map.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_data_asset",
    "output_schema": null
  },
  {
    "category": "data asset",
    "description": "[data asset] Create a new UDataAsset instance. `class_name` is the short C++ class name (or BP path) of a UDataAsset subclass.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "class_name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "class_name"
      ],
      "type": "object"
    },
    "name": "create_data_asset",
    "output_schema": null
  },
  {
    "category": "data asset",
    "description": "[data asset] Set a UPROPERTY on a UDataAsset instance. `value` is the text form UE's property system uses.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "property_name": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "property_name",
        "value"
      ],
      "type": "object"
    },
    "name": "set_data_asset_property",
    "output_schema": null
  },
  {
    "category": "state tree",
    "description": "[state tree] List UStateTree assets under a content path (default `/Game`).",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_state_trees",
    "output_schema": null
  },
  {
    "category": "state tree",
    "description": "[state tree] Read a UStateTree's hierarchy + transitions: every state (id, name, parent) and every transition (from, to, trigger).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_state_tree",
    "output_schema": null
  },
  {
    "category": "state tree",
    "description": "[state tree] Add a state to a UStateTree. NOT to be confused with `add_anim_state` (AnimBP state machine). Empty `parent_state_id` makes it a top-level state. Returns the new state id.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "parent_state_id": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "name"
      ],
      "type": "object"
    },
    "name": "add_state_tree_state",
    "output_schema": null
  },
  {
    "category": "state tree",
    "description": "[state tree] Define a transition between two UStateTree states. `trigger` names the event class or tick condition (e.g. `OnTick`, `OnEvent.Damage`).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "from_state_id": {
          "type": "string"
        },
        "to_state_id": {
          "type": "string"
        },
        "trigger": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "from_state_id",
        "to_state_id",
        "trigger"
      ],
      "type": "object"
    },
    "name": "set_state_tree_transition",
    "output_schema": null
  },
  {
    "category": "state tree",
    "description": "[state tree] Compile a UStateTree. Returns `{compiled: true|false}` — false means compile failed; check `read_output_log` for errors.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "compile_state_tree",
    "output_schema": null
  },
  {
    "category": "profiling",
    "description": "[profiling] Start a profiling capture. `mode` selects the backend: `stats` (UE's built-in stat group file, default), `insights` (UnrealInsights trace), or `csv` (CSVProfiler). Returns `{started, output_file}` — the file path may be empty until `stop_profile` finalizes the capture.",
    "input_schema": {
      "properties": {
        "mode": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "start_profile",
    "output_schema": null
  },
  {
    "category": "profiling",
    "description": "[profiling] Stop the active profile capture and return its output file path. No-op if nothing is in progress.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "stop_profile",
    "output_schema": null
  },
  {
    "category": "profiling",
    "description": "[profiling] Toggle a stat group overlay in the editor. `group` is the name passed to UE's `stat` command (`Unit`, `Game`, `GPU`, `Memory`). The name reads like a getter but this is actually a stateful TOGGLE — first call enables the overlay, second call with the same `group` disables it. The numeric snapshot is NOT returned; `stat <group>` writes to the engine log overlay, and you need to capture it via `read_output_log` (or screenshot the viewport) after the toggle. Treat as a side-effecting probe, not a read.",
    "input_schema": {
      "properties": {
        "group": {
          "type": "string"
        }
      },
      "required": [
        "group"
      ],
      "type": "object"
    },
    "name": "get_stats",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Capture a high-res screenshot to disk. `dest_path` is the output file; `width`/`height` default to the current viewport size if omitted. Routed via UE's `HighResShot` exec command.\n\nPass `return_inline: true` (default false) to also receive the PNG as an MCP Image content block — capped at 1280px max-dim; larger captures are rejected with a hint to re-capture smaller. BP_READER_NEVER_INLINE_IMAGES=1 forces classic path-only behaviour regardless of the arg.",
    "input_schema": {
      "properties": {
        "dest_path": {
          "type": "string"
        },
        "height": {
          "type": "integer"
        },
        "return_inline": {
          "description": "Emit the captured PNG as an Image content block (<=1280px max-dim). Default false.",
          "type": "boolean"
        },
        "width": {
          "type": "integer"
        }
      },
      "required": [
        "dest_path"
      ],
      "type": "object"
    },
    "name": "take_screenshot",
    "output_schema": {
      "properties": {
        "captured": {
          "type": "boolean"
        },
        "image_height": {
          "type": "integer"
        },
        "image_width": {
          "type": "integer"
        },
        "ok": {
          "type": "boolean"
        },
        "output_file": {
          "type": "string"
        }
      },
      "required": [
        "ok",
        "captured",
        "output_file"
      ],
      "type": "object"
    }
  },
  {
    "category": "cook",
    "description": "[cook] Run UE's content cook for a target platform (`Windows`, `Linux`, etc.). Asynchronous; the tool returns once the cook is dispatched. Follow progress via `read_output_log` or the editor's Cook Status panel.",
    "input_schema": {
      "properties": {
        "platform": {
          "type": "string"
        }
      },
      "required": [
        "platform"
      ],
      "type": "object"
    },
    "name": "cook_content",
    "output_schema": null
  },
  {
    "category": "cook",
    "description": "[cook] Package the project for a target platform via UAT. `output_dir` is where the packaged build lands. Async — tool returns once UAT is dispatched.",
    "input_schema": {
      "properties": {
        "output_dir": {
          "type": "string"
        },
        "platform": {
          "type": "string"
        }
      },
      "required": [
        "platform",
        "output_dir"
      ],
      "type": "object"
    },
    "name": "package_project",
    "output_schema": null
  },
  {
    "category": "class info",
    "description": "[class info] Inspect a UClass: parent + ancestor chain + every UPROPERTY + UFUNCTION. `class_name` is the short class name (e.g. `Actor`, `PlayerController`) or a full class path.\n\nBy default, only members **declared on this class** are returned — inherited members are filtered out so the response stays small (UCharacter alone inherits ~100+ properties from Actor / Pawn / UObject). Pass `include_inherited: true` to get the full transitive surface; walk the `ancestors` chain with repeat `get_class_info` calls when you want layered detail.",
    "input_schema": {
      "properties": {
        "class_name": {
          "type": "string"
        },
        "include_inherited": {
          "description": "Include properties and functions inherited from ancestor classes. Default false (own members only) to keep responses focused; set true for the full transitive surface.",
          "type": "boolean"
        }
      },
      "required": [
        "class_name"
      ],
      "type": "object"
    },
    "name": "get_class_info",
    "output_schema": null
  },
  {
    "category": "class info",
    "description": "[class info] Search the UClass registry by substring. Returns an array of class names matching `query` (case-insensitive). Compiler-generated companion classes (`SKEL_*`, `REINST_*`, `TRASHCLASS_*`) are filtered by default — pass `include_skeleton: true` to include them.",
    "input_schema": {
      "properties": {
        "include_skeleton": {
          "description": "Include compiler-generated companion classes (SKEL_*, REINST_*, TRASHCLASS_*). Default false.",
          "type": "boolean"
        },
        "query": {
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "name": "find_class",
    "output_schema": null
  },
  {
    "category": "class info",
    "description": "[class info] List every UFUNCTION on a class with its flags (BlueprintCallable, BlueprintPure, etc.). Cheaper projection than `get_class_info` when you only need the call surface.",
    "input_schema": {
      "properties": {
        "class_name": {
          "type": "string"
        }
      },
      "required": [
        "class_name"
      ],
      "type": "object"
    },
    "name": "list_functions",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Frame an actor in the editor viewport — equivalent to clicking the actor and pressing F. `actor_name` is the actor's level label.",
    "input_schema": {
      "properties": {
        "actor_name": {
          "type": "string"
        }
      },
      "required": [
        "actor_name"
      ],
      "type": "object"
    },
    "name": "focus_actor",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Move the editor viewport camera to a specific location + rotation. Rotation is in degrees (pitch / yaw / roll).",
    "input_schema": {
      "properties": {
        "loc_x": {
          "type": "number"
        },
        "loc_y": {
          "type": "number"
        },
        "loc_z": {
          "type": "number"
        },
        "rot_pitch": {
          "type": "number"
        },
        "rot_roll": {
          "type": "number"
        },
        "rot_yaw": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "name": "set_camera_transform",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Quick capture of the active editor viewport to disk (vs `take_screenshot` which uses HighResShot for offline-quality output).\n\nPass `return_inline: true` (default false) to also receive the PNG as an MCP Image content block — capped at 1280px max-dim; larger captures are rejected with a hint to re-capture smaller. BP_READER_NEVER_INLINE_IMAGES=1 forces classic path-only behaviour.",
    "input_schema": {
      "properties": {
        "dest_path": {
          "type": "string"
        },
        "return_inline": {
          "description": "Emit the captured PNG as an Image content block (<=1280px max-dim). Default false.",
          "type": "boolean"
        }
      },
      "required": [
        "dest_path"
      ],
      "type": "object"
    },
    "name": "take_viewport_screenshot",
    "output_schema": {
      "properties": {
        "captured": {
          "type": "boolean"
        },
        "image_height": {
          "type": "integer"
        },
        "image_width": {
          "type": "integer"
        },
        "ok": {
          "type": "boolean"
        },
        "output_file": {
          "type": "string"
        }
      },
      "required": [
        "ok",
        "captured",
        "output_file"
      ],
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Capture the viewport + return structured spatial metadata (camera transform, selected actors, optional editor state) so a vision-capable agent can reason about scene contents without separate `get_*` calls. The image is written to `dest_path` (same as take_viewport_screenshot); the annotation data is the structured metadata returned alongside. `annotation_overlay_rendered` is false in this version — image is a raw capture, future revisions may add a projected 3D grid + actor labels drawn into the image itself.",
    "input_schema": {
      "properties": {
        "dest_path": {
          "description": "Output file path for the captured image.",
          "type": "string"
        },
        "include_editor_state": {
          "description": "Include editor state (camera, level, PIE status). Default true.",
          "type": "boolean"
        },
        "include_selected_actors": {
          "description": "Include selected actor metadata. Default true.",
          "type": "boolean"
        }
      },
      "required": [
        "dest_path"
      ],
      "type": "object"
    },
    "name": "take_annotated_screenshot",
    "output_schema": {
      "properties": {
        "annotation_overlay_rendered": {
          "type": "boolean"
        },
        "camera": {
          "properties": {
            "fov": {
              "type": "number"
            },
            "location": {
              "description": "[x,y,z] cm",
              "items": {
                "type": "number"
              },
              "type": "array"
            },
            "rotation": {
              "description": "[pitch,yaw,roll] deg",
              "items": {
                "type": "number"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "captured": {
          "type": "boolean"
        },
        "editor_state": {
          "type": "object"
        },
        "ok": {
          "type": "boolean"
        },
        "output_file": {
          "type": "string"
        },
        "selected_actors": {
          "items": {
            "properties": {
              "class": {
                "type": "string"
              },
              "label": {
                "type": "string"
              },
              "location": {
                "items": {
                  "type": "number"
                },
                "type": "array"
              },
              "name": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "ok",
        "captured",
        "output_file",
        "annotation_overlay_rendered"
      ],
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Toggle a viewport show flag (`Bones`, `Bounds`, `Collision`, `Wireframe`, `Lighting`). Equivalent to the `showflag.<name> <0|1>` console command.",
    "input_schema": {
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "flag_name": {
          "type": "string"
        }
      },
      "required": [
        "flag_name",
        "enabled"
      ],
      "type": "object"
    },
    "name": "set_show_flag",
    "output_schema": null
  },
  {
    "category": "editor",
    "description": "[editor] Async asset-compilation backlog (textures, static meshes, etc.) via FAssetCompilingManager. `remaining_assets` is the aggregate still compiling; 0 means idle. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_async_compile_state",
    "output_schema": {
      "properties": {
        "remaining_assets": {
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Shader-compilation backlog via GShaderCompilingManager: `{is_compiling, outstanding_jobs, pending_jobs}`. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_shader_compile_state",
    "output_schema": {
      "properties": {
        "is_compiling": {
          "type": "boolean"
        },
        "outstanding_jobs": {
          "type": "integer"
        },
        "pending_jobs": {
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] The editor's current level (where newly-spawned actors land) + owning world. Names are package paths. `valid:false` means no editor world. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_current_level",
    "output_schema": {
      "properties": {
        "level_name": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        },
        "world_name": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] All loaded levels (persistent + streaming sublevels) as package paths. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_loaded_levels",
    "output_schema": {
      "properties": {
        "level_names": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active source-control provider: `{name, enabled, available}`. `name` is e.g. \"Git\"/\"Perforce\"/\"None\". Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_source_control_provider",
    "output_schema": {
      "properties": {
        "available": {
          "type": "boolean"
        },
        "enabled": {
          "type": "boolean"
        },
        "name": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Asset-registry scan status: `{is_loading_assets, search_all_assets}`. `is_loading_assets:true` means the background scan is still running (asset queries may be incomplete). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_asset_registry_state",
    "output_schema": {
      "properties": {
        "is_loading_assets": {
          "type": "boolean"
        },
        "search_all_assets": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] World Partition data layers + per-layer effective runtime state (Unloaded/Loaded/Activated). Each: `{short_name, full_name, runtime_state}`. `has_world_partition:false` on non-partitioned maps (layers empty). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_data_layer_states",
    "output_schema": {
      "properties": {
        "has_world_partition": {
          "type": "boolean"
        },
        "layers": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Editor autosave status: `{is_auto_saving}` (true while an autosave is in progress). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_autosave_status",
    "output_schema": {
      "properties": {
        "is_auto_saving": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Crash-recovery state: `{has_packages_to_restore}` (true when autosave restore files are pending from a prior unclean shutdown). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_recovery_state",
    "output_schema": {
      "properties": {
        "has_packages_to_restore": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Per-file source-control status (cached): `{valid, controlled, checked_out, checked_out_other, modified, current}`. `valid:false` when SCC is disabled or the file has no cached state. Reads cached state only (no server round-trip). Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_source_control_status",
    "output_schema": {
      "properties": {
        "checked_out": {
          "type": "boolean"
        },
        "checked_out_other": {
          "type": "boolean"
        },
        "controlled": {
          "type": "boolean"
        },
        "current": {
          "type": "boolean"
        },
        "modified": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Whether an asset is checked out / locked by another user: `{valid, checked_out_by_other, other_user}`. Reads cached SCC state. Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_file_lock_status",
    "output_schema": {
      "properties": {
        "checked_out_by_other": {
          "type": "boolean"
        },
        "other_user": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active editor culture/language: `{language, culture, display_name}` (e.g. en / en-US / \"English (United States)\"). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_active_culture",
    "output_schema": {
      "properties": {
        "culture": {
          "type": "string"
        },
        "display_name": {
          "type": "string"
        },
        "language": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Current editor theme id (`UEditorStyleSettings::CurrentAppliedTheme` GUID). Dark/Light ship with fixed GUIDs; custom themes get their own. Agents can detect theme changes by id. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_editor_theme",
    "output_schema": {
      "properties": {
        "theme_id": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Connected monitors (FDisplayMetrics): each `{name, native_width, native_height, is_primary}`. Useful for multi-monitor placement reasoning. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_monitor_info",
    "output_schema": {
      "properties": {
        "monitors": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Live Coding (C++ hot-patch) state: `{available, has_started, is_compiling}`. `available:false` on non-Windows or when the module isn't loaded. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_live_coding_state",
    "output_schema": {
      "properties": {
        "available": {
          "type": "boolean"
        },
        "has_started": {
          "type": "boolean"
        },
        "is_compiling": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] World Partition streaming sources (camera/player providers driving cell streaming). Each: `{name, loc_x/y/z, pitch, yaw, roll}`. `has_world_partition:false` on non-partitioned maps (sources empty). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_streaming_sources",
    "output_schema": {
      "properties": {
        "has_world_partition": {
          "type": "boolean"
        },
        "sources": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Recently-opened assets from the editor MRU list, most-recent first (package paths). Reflects the current editor session's history. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_recently_opened_assets",
    "output_schema": {
      "properties": {
        "asset_paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Enable or disable a plugin in the project's .uproject descriptor (IProjectManager). `applied` = the descriptor changed; `saved` = the .uproject was written. Takes effect on the next editor restart (modules load/unload at startup). Blocked in read-only mode (mutates the project file). Requires a live editor.",
    "input_schema": {
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "plugin": {
          "type": "string"
        }
      },
      "required": [
        "plugin",
        "enabled"
      ],
      "type": "object"
    },
    "name": "set_plugin_enabled",
    "output_schema": {
      "properties": {
        "applied": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        },
        "saved": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Packages saved during this editor session, most-recent first (package paths). Backed by a ring buffer populated from the package-saved event — empty in a fresh headless commandlet, accumulates in a live editor. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_recently_saved_packages",
    "output_schema": {
      "properties": {
        "package_paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Request activation of a Game Feature Plugin by name or file:-protocol URL (async, fire-and-forget). `requested:true` means load+activate was queued; poll `get_game_feature_state` to confirm. `requested:false` means the name didn't resolve to a GFP. Blocked in read-only mode. Requires a live editor.",
    "input_schema": {
      "properties": {
        "plugin": {
          "type": "string"
        }
      },
      "required": [
        "plugin"
      ],
      "type": "object"
    },
    "name": "activate_game_feature",
    "output_schema": {
      "properties": {
        "ok": {
          "type": "boolean"
        },
        "requested": {
          "type": "boolean"
        },
        "url": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Request deactivation of a Game Feature Plugin by name or file:-protocol URL (async, fire-and-forget). `requested:true` means deactivation was queued; poll `get_game_feature_state` to confirm. `requested:false` means the name didn't resolve. Blocked in read-only mode. Requires a live editor.",
    "input_schema": {
      "properties": {
        "plugin": {
          "type": "string"
        }
      },
      "required": [
        "plugin"
      ],
      "type": "object"
    },
    "name": "deactivate_game_feature",
    "output_schema": {
      "properties": {
        "ok": {
          "type": "boolean"
        },
        "requested": {
          "type": "boolean"
        },
        "url": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Stat overlays enabled in the active level viewport (e.g. `Unit`, `FPS`, `GPU`, `SceneRendering`) via FViewportClient::GetEnabledStats. `valid:false` when no viewport is focused. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_active_stats",
    "output_schema": {
      "properties": {
        "stats": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Watched pins on a Blueprint (FKismetDebugUtilities). Each: `{pin_name, node_guid, node_name, direction}`. `valid:false` when the BP can't be loaded. Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_watched_pins",
    "output_schema": {
      "properties": {
        "pins": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Breakpoints set on a Blueprint (FKismetDebugUtilities). Each: `{node_guid, node_name, location, enabled}`. `valid:false` when the BP can't be loaded. Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_blueprint_breakpoints",
    "output_schema": {
      "properties": {
        "breakpoints": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] PIE-attached debug object for a Blueprint (`UBlueprint::GetObjectBeingDebugged`): `{valid, has_debug_object, debug_object_name, debug_object_path}`. `has_debug_object:false` when nothing is attached (e.g. not in PIE). Requires a live editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "get_debug_instance",
    "output_schema": {
      "properties": {
        "debug_object_name": {
          "type": "string"
        },
        "debug_object_path": {
          "type": "string"
        },
        "has_debug_object": {
          "type": "boolean"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] All Project/Editor Settings sections (UDeveloperSettings CDOs): each `{container, category, section, class_path}`. The 3-tier nav read (Container -> Category -> Section) that future get/set/save/reset settings tools drill into. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_project_settings",
    "output_schema": {
      "properties": {
        "sections": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] All property values of one settings section (UDeveloperSettings CDO), each `{name, value, type}` where `value` is the reflection-exported text. `class_path` comes from `list_project_settings`. `valid:false` when the class can't be resolved. Requires a live editor.",
    "input_schema": {
      "properties": {
        "class_path": {
          "type": "string"
        }
      },
      "required": [
        "class_path"
      ],
      "type": "object"
    },
    "name": "get_project_setting_values",
    "output_schema": {
      "properties": {
        "class_path": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        },
        "values": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Set one property on a settings section by `class_path` + `property` name, importing `value` from text, then persisting to the class's Default*.ini. `applied:true` on success; `message` carries the failure detail otherwise. Best for scalar values (numbers/bools/enums/simple strings) — complex struct text may not survive arg parsing. Blocked in read-only mode. Requires a live editor.",
    "input_schema": {
      "properties": {
        "class_path": {
          "type": "string"
        },
        "property": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "class_path",
        "property",
        "value"
      ],
      "type": "object"
    },
    "name": "set_project_setting",
    "output_schema": {
      "properties": {
        "applied": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        },
        "ok": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Registered automation tests (FAutomationTestFramework, synchronous). Each `{display_name, full_path, test_name}` — `full_path`/`test_name` feed the `pattern` arg of `run_automation_tests`. Capped at 2000 (`truncated:true` if more). Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_automation_tests",
    "output_schema": {
      "properties": {
        "tests": {
          "items": {
            "type": "object"
          },
          "type": "array"
        },
        "truncated": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Drain buffered editor events captured from UE delegates. Covers selection changes, asset open/remove/rename, PIE start/stop/pause/resume, package save, map open, actor add/delete, and Blueprint compile — each `{name, params}`. Draining clears the buffer. Empty in a one-shot commandlet; accumulates in a live/daemon editor and is also auto-pushed over the HTTP SSE stream as notifications. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_editor_events",
    "output_schema": {
      "properties": {
        "events": {
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] List the editor's active cook target platforms and the running platform, via ITargetPlatformManagerModule. `platforms` is the set UBT/the cooker would target; `running_platform` is the host the editor itself runs on (e.g. Windows). Empty when the TargetPlatform module is unavailable. Requires a live editor.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_active_cook_target",
    "output_schema": {
      "properties": {
        "platforms": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "running_platform": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] The editor's current docking/workspace layout, serialized via FGlobalTabmanager::PersistLayout (`layout` string). Reflects which tabs/panels are open and how they're arranged. Empty when Slate isn't initialized (e.g. a -nullrhi commandlet); populated in a live editor. Requires a live editor for meaningful output.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_workspace_layout",
    "output_schema": {
      "properties": {
        "layout": {
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Unreal Insights trace connection state via FTraceAuxiliary (Core): `connected`, `paused`, `connection_type` (network|file|none), `destination` (host or file path), and `active_channels` (comma-separated enabled trace channels). Reports the responding process's own trace state (the live editor when routed live; the commandlet otherwise).",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_trace_state",
    "output_schema": {
      "properties": {
        "active_channels": {
          "type": "string"
        },
        "connected": {
          "type": "boolean"
        },
        "connection_type": {
          "type": "string"
        },
        "destination": {
          "type": "string"
        },
        "paused": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] World Outliner UI state (search text, type filter, row expansion, columns). v1 stub: outliner state lives in transient Slate widgets not bridged out-of-process — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_outliner_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Actors pinned in the World Outliner. v1 stub: pin state is Slate-widget-local and not bridged out-of-process — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_pinned_actors",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Details panel UI state (search filter, expanded categories, pinned properties). v1 stub: this is transient SDetailsView state not bridged out-of-process — returns valid:false. (Selected objects are available via get_selected_actors.)",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_details_panel_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active status-bar message text. v1 stub: the status bar API is push-only (no public getter) — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_status_bar_messages",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Active toast notifications (FSlateNotificationManager). v1 stub: the manager exposes no public enumeration of live notifications — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_active_notifications",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Modeling Tools Editor Mode state (active tool, sub-element selection mode: vertex/edge/face/group). v1 stub: this is interactive-tools-context state internal to the mode manager, not bridged out-of-process — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_modeling_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Landscape edit mode paint state (active brush/target layer). v1 stub: landscape mode state is not bridged out-of-process — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_landscape_paint_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Foliage edit mode paint state (selected foliage types, brush settings). v1 stub: foliage mode state is not bridged out-of-process — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_foliage_paint_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Mesh paint mode state (color/weight/texture sub-mode, selected channel). v1 stub: mesh-paint mode state is not bridged out-of-process — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_mesh_paint_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Texture paint mode state (selected texture/UV channel). v1 stub: texture-paint mode state is not bridged out-of-process — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_texture_paint_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] In-editor cook progress. v1 stub: no in-editor cook session exists to report by default, and there's no clean progress API — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_cook_progress",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Derived Data Cache state/usage. v1 stub: the DDC interface exposes only get/put, no public usage-stats accessor — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_ddc_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Static lighting build progress. v1 stub: no clean global lighting-build progress accessor — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_lighting_build_progress",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Set the active cook target platforms. v1 stub: ITargetPlatformManager has no runtime setter (active platforms derive from the launch command line) — returns valid:false. Read the current set with get_active_cook_target.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "set_active_cook_target",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Loaded World Partition runtime cells. v1 stub: cell enumeration uses partly-internal WorldPartition runtime-hash APIs not yet wired — returns valid:false. (get_streaming_sources + get_world_partition_state cover the queryable parts.)",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_loaded_partition_cells",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Source-control changelists. v1 stub: changelists are Perforce-centric (ISourceControlProvider::GetChangelists returns empty for Git/None); a real enumeration is pending — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "list_changelists",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] The default/pending source-control changelist. v1 stub: Perforce-centric (empty for Git/None) — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_pending_changelist",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Take Recorder state (recording active, current take). v1 stub: a real implementation must link the TakeRecorder plugin module (UTakeRecorder::GetActiveRecorder) — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_take_recorder_state",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Movie Render Queue pending jobs. v1 stub: a real implementation must link the MovieRenderPipeline plugin module — returns valid:false.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "get_render_queue",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "editor",
    "description": "[editor] Reset a project setting to its default. v1 stub: a correct factory-reset needs the editor details-panel's default-value resolution — a config property's constructor-set default is unrecoverable out-of-process after config load (RemoveKey+ReloadConfig only drops the persisted override, leaving the in-memory value unchanged). Use set_project_setting to assign explicit values; get/set/save round-trip works.",
    "input_schema": {
      "properties": {},
      "type": "object"
    },
    "name": "reset_project_setting",
    "output_schema": {
      "properties": {
        "reason": {
          "type": "string"
        },
        "valid": {
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "category": "niagara",
    "description": "[niagara] List UNiagaraSystem assets under a content path (default `/Game`).",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_niagara_systems",
    "output_schema": null
  },
  {
    "category": "niagara",
    "description": "[niagara] Read a UNiagaraSystem's emitter handles (each names an underlying UNiagaraEmitter) and its exposed user parameter names.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_niagara_system",
    "output_schema": null
  },
  {
    "category": "niagara",
    "description": "[niagara] Create a new (empty) UNiagaraSystem asset at the given path. Idempotent.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "create_niagara_system",
    "output_schema": null
  },
  {
    "category": "niagara",
    "description": "[niagara] Override a user-exposed parameter on a UNiagaraSystem. `value` is the parameter's text form.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "parameter_name": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "parameter_name",
        "value"
      ],
      "type": "object"
    },
    "name": "set_niagara_parameter",
    "output_schema": null
  },
  {
    "category": "level sequence",
    "description": "[level sequence] List ULevelSequence assets under a content path.",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_level_sequences",
    "output_schema": null
  },
  {
    "category": "level sequence",
    "description": "[level sequence] Read a sequence's playback range (start/end seconds) and its top-level tracks.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_level_sequence",
    "output_schema": null
  },
  {
    "category": "level sequence",
    "description": "[level sequence] Add a master track to a ULevelSequence (Sequencer). `track_class` is the short class name (`MovieSceneAudioTrack`, `MovieScene3DTransformTrack`).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "track_class": {
          "type": "string"
        },
        "track_name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "track_class",
        "track_name"
      ],
      "type": "object"
    },
    "name": "add_sequence_track",
    "output_schema": null
  },
  {
    "category": "level sequence",
    "description": "[level sequence] Set the playback range of a ULevelSequence in seconds (start <= end).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "end_seconds": {
          "type": "number"
        },
        "start_seconds": {
          "type": "number"
        }
      },
      "required": [
        "asset_path",
        "start_seconds",
        "end_seconds"
      ],
      "type": "object"
    },
    "name": "set_sequence_playback_range",
    "output_schema": null
  },
  {
    "category": "gameplay tag",
    "description": "[gameplay tag] Query the project's GameplayTagsManager. `filter` is an optional substring; empty returns every registered tag.",
    "input_schema": {
      "properties": {
        "filter": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_gameplay_tags",
    "output_schema": null
  },
  {
    "category": "gameplay tag",
    "description": "[gameplay tag] Add a tag to the project's gameplay tag dictionary. `name` uses dot-separated form (e.g. `Status.Damage.Fire`). `comment` is optional.",
    "input_schema": {
      "properties": {
        "comment": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "name": "add_gameplay_tag",
    "output_schema": null
  },
  {
    "category": "gameplay tag",
    "description": "[gameplay tag] Read a GAS ability-set DataAsset: every granted ability class + its level.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_ability_set",
    "output_schema": null
  },
  {
    "category": "anim",
    "description": "[anim] List UAnimBlueprint assets under a content path.",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "name": "list_anim_blueprints",
    "output_schema": null
  },
  {
    "category": "anim",
    "description": "[anim] Walk a UAnimBlueprint: parent class + each state machine's states (state / conduit / transition / entry).",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "read_anim_blueprint",
    "output_schema": null
  },
  {
    "category": "anim",
    "description": "[anim] Add a state to a named state machine inside a UAnimBlueprint. NOT to be confused with `add_state_tree_state` (StateTree). Scaffold only — final state authoring still uses the AnimGraph editor.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "state_machine": {
          "type": "string"
        },
        "state_name": {
          "type": "string"
        }
      },
      "required": [
        "asset_path",
        "state_machine",
        "state_name"
      ],
      "type": "object"
    },
    "name": "add_anim_state",
    "output_schema": null
  },
  {
    "category": "anim",
    "description": "[anim] Compile a UAnimBlueprint.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        }
      },
      "required": [
        "asset_path"
      ],
      "type": "object"
    },
    "name": "compile_anim_blueprint",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Compare two Blueprints structurally — variables, components, function/macro/event-graph node signatures, connection counts. Returns {ok, differences[]}. Position- and GUID-independent so a freshly-rebuilt clone diffs cleanly against its source. Requires live or commandlet backend.",
    "input_schema": {
      "properties": {
        "candidate": {
          "description": "Clone BP package path",
          "type": "string"
        },
        "options": {
          "properties": {
            "ignore_comment_nodes": {
              "type": "boolean"
            },
            "ignore_node_positions": {
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "source": {
          "description": "Source BP package path",
          "type": "string"
        }
      },
      "required": [
        "source",
        "candidate"
      ],
      "type": "object"
    },
    "name": "bp_structural_diff",
    "output_schema": null
  },
  {
    "category": "blueprint",
    "description": "[blueprint] Execute a batch of write operations sequentially in a single tool call. Each op is `{op:\"add_variable\"|...|\"wire_pins\", ...args}`. Reduces N round-trips and N agent reasoning steps to one.\n\n**When to prefer over single-shot mutators:** any time you need >2 mutations on the same BP. The single-recompile-per-op limitation (below) still applies, but you save N–1 tool-call round-trips and the named-slot system means you don't have to thread GUIDs through your reasoning. For 1–2 mutations, the individual tools (`add_node`, `wire_pins`, etc.) are fine and easier to read.\n\nNamed slots: an `add_node` op may carry `id: \"<name>\"`. Subsequent ops can reference that node's GUID with `\"$<name>\"` or `{\"ref\":\"<name>\"}` in any node-id field. Eliminates the need to thread GUIDs through the agent's reasoning.\n\n`atomic: true` (default) bails on first error. `atomic: false` continues; failed ops appear as `{ok: false, error: \"...\"}` in the per-op results array, succeeded ops as their normal payload.\n\nLimitation: each underlying op still saves + recompiles the BP individually. True single-recompile batching needs plugin work — tracked separately.",
    "input_schema": {
      "properties": {
        "atomic": {
          "description": "Bail on first failure (default true).",
          "type": "boolean"
        },
        "on_failure": {
          "description": "What EndBatch does after a mid-batch failure. \"compile\" (default): best-effort — compile + save what landed before the failure. Matches today's per-op behavior. \"skip\": don't compile, don't save — nothing reaches disk. The in-memory daemon state stays dirty until restart, so subsequent reads in the same session can see the partial mutations; this is a documented limitation of strict-atomic mode.",
          "enum": [
            "compile",
            "skip"
          ],
          "type": "string"
        },
        "ops": {
          "items": {
            "additionalProperties": true,
            "properties": {
              "id": {
                "description": "Optional slot name. For node-spawning ops, binds the new GUID under this name for later refs.",
                "type": "string"
              },
              "op": {
                "type": "string"
              }
            },
            "required": [
              "op"
            ],
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "ops"
      ],
      "type": "object"
    },
    "name": "apply_ops",
    "output_schema": null
  },
  {
    "category": "other",
    "description": "Validate an apply_ops batch without mutating anything. Walks the op array, parses each op's required fields, resolves named-slot refs against placeholder GUIDs, and uses read-only backend calls to confirm referenced vars/functions exist. Returns per-op `{ok}` results plus a `would_compile` list of asset paths the real apply_ops would touch. Useful for agent self-checks (\"is this batch syntactically valid before I run it?\") and for a human-in-the-loop confirmation step.",
    "input_schema": {
      "properties": {
        "ops": {
          "description": "Same shape as apply_ops's `ops` field.",
          "items": {
            "additionalProperties": true,
            "properties": {
              "op": {
                "type": "string"
              }
            },
            "required": [
              "op"
            ],
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "ops"
      ],
      "type": "object"
    },
    "name": "preview_ops",
    "output_schema": null
  },
  {
    "category": "cpp",
    "description": "[cpp] Compile a tiny pseudocode DSL into a fully-wired Blueprint function. Accepts the function signature plus a `body` of statements; translates to add_node + wire_pins + set_pin_default ops and runs them as one batch (single recompile per affected BP).\n\nStatements: {if, then, [else]}, {set, to}, {call, args}, {comment}.\nExpressions:\n  {var:\"name\"}     — VariableGet for a member variable\n  {lit:value}      — literal pin default (string / number / bool)\n  {call:\"fn\", args:{...}} — CallFunction node\n\n`call` accepts operator aliases that map to UKismetMathLibrary:\n  +, -, *, /, %, ==, !=, <, <=, >, >=, &&, ||, !\n  Float-explicit variants: +f, -f, *f, /f, ==f, <f, <=f\nOr use canonical \"Owner::Function\" form for any other call.\n\nAfter an if/then/else, exec from BOTH branches converges into the next statement (UE's K2 schema accepts multiple sources on exec input pins — no Sequence node needed). If a branch's body is empty, that side's exec out from the Branch node is the tail.\n\nPass `dry_run: true` to get the compiled op list without executing — useful for inspecting what compile_function would do before committing.\n\nOn expression/statement form errors, the response says exactly which form was unrecognized so the agent can fall back to apply_ops for that statement only.",
    "input_schema": {
      "properties": {
        "asset_path": {
          "type": "string"
        },
        "atomic": {
          "description": "Bail on first op failure (default true).",
          "type": "boolean"
        },
        "body": {
          "description": "Sequence of statement objects — see tool description.",
          "type": "array"
        },
        "dry_run": {
          "description": "Return the compiled ops without executing — lets you preview.",
          "type": "boolean"
        },
        "function_name": {
          "type": "string"
        },
        "inputs": {
          "description": "[{name, type}, ...] — same shape as add_function_input",
          "type": "array"
        },
        "outputs": {
          "description": "[{name, type}, ...] — same shape as add_function_output",
          "type": "array"
        }
      },
      "required": [
        "asset_path",
        "function_name",
        "body"
      ],
      "type": "object"
    },
    "name": "compile_function",
    "output_schema": null
  }
]
